// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: composing.proto
// Protobuf Java Version: 4.27.5

package com.siansiansu.taigikeyboard.engine.proto;

/**
 * <pre>
 * v3.5.8 Phase 6 — single span-local continuous candidate. Wire mirror of
 * `lexicon::RawCandidate` (`engine/lexicon/src/continuous.rs:84-124`).
 *
 * `consumed_span` is encoded as two scalar fields rather than a nested
 * message to keep wire overhead low. `consumed_span_start` /
 * `consumed_span_end` are byte offsets in the **original raw user
 * input** stored in `Phase::Continuous { raw }` — not in any canonical
 * TL transform. TL / POJ users → ASCII bytes; TPS users → Bopomofo
 * bytes (Phase 6 dispatcher converts TPS spans to canonical TL FST
 * keys for lookup but emits the span back in the user-facing TPS byte
 * space). Platform UI slices `pending[start..end]` off
 * `Phase::Continuous { raw }` (or its preedit mirror) on commit.
 *
 * `form` is currently always `1` (FORM_NOTONE; see
 * `engine/lexicon/src/continuous.rs:78`); reserved for hanzi (0) /
 * numeric (2) / abbrev (3) when proto-side carriers exist.
 *
 * `mode` (Phase 9.2) is the MOE-aligned candidate-type discriminator;
 * see `CandidateMode` above.
 *
 * v3.5.8 Phase 9 Item 5 — `roman` + `hanji` are display-only
 * sidechannels added so platform UI can build dual-line cells
 * (roman / hanji) the same way the legacy lexicon path does. The
 * engine continues to authority-stamp commit semantics and
 * `user_frequency.db` write keys via `display_text`
 * (= `hanji.unwrap_or(roman)`); `roman` and `hanji` are NEVER read
 * for commit. `roman` is the display romanization for the active
 * input mode — the underlying `DictionaryRecord.tl` by default, or
 * POJ-display (`oo`→`o͘`, `nn`→`ⁿ`, …) when
 * `AppConfig.input_mode = poj` (engine-rendered, mirroring the
 * dictionary-search path); `hanji` mirrors `DictionaryRecord.hanzi`
 * (proto3 `optional` distinguishes "TAILO candidate — no hanji
 * exists" from "wire-frame defect"). See
 * `docs/engine/continuous-candidate-display.md` §4 for the full
 * rationale and `docs/engine/continuous-input-ranking.md` §10.11
 * for the companion ranker dedupe rule.
 * </pre>
 *
 * Protobuf type {@code taigi.engine.CandidateMessage}
 */
public  final class CandidateMessage extends
    com.google.protobuf.GeneratedMessageLite<
        CandidateMessage, CandidateMessage.Builder> implements
    // @@protoc_insertion_point(message_implements:taigi.engine.CandidateMessage)
    CandidateMessageOrBuilder {
  private CandidateMessage() {
    displayText_ = "";
    roman_ = "";
    hanji_ = "";
    canonicalTl_ = "";
  }
  private int bitField0_;
  public static final int CONSUMED_SPAN_START_FIELD_NUMBER = 1;
  private int consumedSpanStart_;
  /**
   * <code>uint32 consumed_span_start = 1;</code>
   * @return The consumedSpanStart.
   */
  @java.lang.Override
  public int getConsumedSpanStart() {
    return consumedSpanStart_;
  }
  /**
   * <code>uint32 consumed_span_start = 1;</code>
   * @param value The consumedSpanStart to set.
   */
  private void setConsumedSpanStart(int value) {

    consumedSpanStart_ = value;
  }
  /**
   * <code>uint32 consumed_span_start = 1;</code>
   */
  private void clearConsumedSpanStart() {

    consumedSpanStart_ = 0;
  }

  public static final int CONSUMED_SPAN_END_FIELD_NUMBER = 2;
  private int consumedSpanEnd_;
  /**
   * <code>uint32 consumed_span_end = 2;</code>
   * @return The consumedSpanEnd.
   */
  @java.lang.Override
  public int getConsumedSpanEnd() {
    return consumedSpanEnd_;
  }
  /**
   * <code>uint32 consumed_span_end = 2;</code>
   * @param value The consumedSpanEnd to set.
   */
  private void setConsumedSpanEnd(int value) {

    consumedSpanEnd_ = value;
  }
  /**
   * <code>uint32 consumed_span_end = 2;</code>
   */
  private void clearConsumedSpanEnd() {

    consumedSpanEnd_ = 0;
  }

  public static final int SYLLABLE_COUNT_FIELD_NUMBER = 3;
  private int syllableCount_;
  /**
   * <code>uint32 syllable_count = 3;</code>
   * @return The syllableCount.
   */
  @java.lang.Override
  public int getSyllableCount() {
    return syllableCount_;
  }
  /**
   * <code>uint32 syllable_count = 3;</code>
   * @param value The syllableCount to set.
   */
  private void setSyllableCount(int value) {

    syllableCount_ = value;
  }
  /**
   * <code>uint32 syllable_count = 3;</code>
   */
  private void clearSyllableCount() {

    syllableCount_ = 0;
  }

  public static final int DISPLAY_TEXT_FIELD_NUMBER = 4;
  private java.lang.String displayText_;
  /**
   * <code>string display_text = 4;</code>
   * @return The displayText.
   */
  @java.lang.Override
  public java.lang.String getDisplayText() {
    return displayText_;
  }
  /**
   * <code>string display_text = 4;</code>
   * @return The bytes for displayText.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getDisplayTextBytes() {
    return com.google.protobuf.ByteString.copyFromUtf8(displayText_);
  }
  /**
   * <code>string display_text = 4;</code>
   * @param value The displayText to set.
   */
  private void setDisplayText(
      java.lang.String value) {
    java.lang.Class<?> valueClass = value.getClass();

    displayText_ = value;
  }
  /**
   * <code>string display_text = 4;</code>
   */
  private void clearDisplayText() {

    displayText_ = getDefaultInstance().getDisplayText();
  }
  /**
   * <code>string display_text = 4;</code>
   * @param value The bytes for displayText to set.
   */
  private void setDisplayTextBytes(
      com.google.protobuf.ByteString value) {
    checkByteStringIsUtf8(value);
    displayText_ = value.toStringUtf8();

  }

  public static final int SCORE_FIELD_NUMBER = 5;
  private float score_;
  /**
   * <code>float score = 5;</code>
   * @return The score.
   */
  @java.lang.Override
  public float getScore() {
    return score_;
  }
  /**
   * <code>float score = 5;</code>
   * @param value The score to set.
   */
  private void setScore(float value) {

    score_ = value;
  }
  /**
   * <code>float score = 5;</code>
   */
  private void clearScore() {

    score_ = 0F;
  }

  public static final int FORM_FIELD_NUMBER = 6;
  private int form_;
  /**
   * <code>uint32 form = 6;</code>
   * @return The form.
   */
  @java.lang.Override
  public int getForm() {
    return form_;
  }
  /**
   * <code>uint32 form = 6;</code>
   * @param value The form to set.
   */
  private void setForm(int value) {

    form_ = value;
  }
  /**
   * <code>uint32 form = 6;</code>
   */
  private void clearForm() {

    form_ = 0;
  }

  public static final int MODE_FIELD_NUMBER = 7;
  private int mode_;
  /**
   * <code>.taigi.engine.CandidateMode mode = 7;</code>
   * @return The enum numeric value on the wire for mode.
   */
  @java.lang.Override
  public int getModeValue() {
    return mode_;
  }
  /**
   * <code>.taigi.engine.CandidateMode mode = 7;</code>
   * @return The mode.
   */
  @java.lang.Override
  public com.siansiansu.taigikeyboard.engine.proto.CandidateMode getMode() {
    com.siansiansu.taigikeyboard.engine.proto.CandidateMode result = com.siansiansu.taigikeyboard.engine.proto.CandidateMode.forNumber(mode_);
    return result == null ? com.siansiansu.taigikeyboard.engine.proto.CandidateMode.UNRECOGNIZED : result;
  }
  /**
   * <code>.taigi.engine.CandidateMode mode = 7;</code>
   * @param value The enum numeric value on the wire for mode to set.
   */
  private void setModeValue(int value) {
      mode_ = value;
  }
  /**
   * <code>.taigi.engine.CandidateMode mode = 7;</code>
   * @param value The mode to set.
   */
  private void setMode(com.siansiansu.taigikeyboard.engine.proto.CandidateMode value) {
    mode_ = value.getNumber();

  }
  /**
   * <code>.taigi.engine.CandidateMode mode = 7;</code>
   */
  private void clearMode() {

    mode_ = 0;
  }

  public static final int ROMAN_FIELD_NUMBER = 8;
  private java.lang.String roman_;
  /**
   * <code>string roman = 8;</code>
   * @return The roman.
   */
  @java.lang.Override
  public java.lang.String getRoman() {
    return roman_;
  }
  /**
   * <code>string roman = 8;</code>
   * @return The bytes for roman.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getRomanBytes() {
    return com.google.protobuf.ByteString.copyFromUtf8(roman_);
  }
  /**
   * <code>string roman = 8;</code>
   * @param value The roman to set.
   */
  private void setRoman(
      java.lang.String value) {
    java.lang.Class<?> valueClass = value.getClass();

    roman_ = value;
  }
  /**
   * <code>string roman = 8;</code>
   */
  private void clearRoman() {

    roman_ = getDefaultInstance().getRoman();
  }
  /**
   * <code>string roman = 8;</code>
   * @param value The bytes for roman to set.
   */
  private void setRomanBytes(
      com.google.protobuf.ByteString value) {
    checkByteStringIsUtf8(value);
    roman_ = value.toStringUtf8();

  }

  public static final int HANJI_FIELD_NUMBER = 9;
  private java.lang.String hanji_;
  /**
   * <code>optional string hanji = 9;</code>
   * @return Whether the hanji field is set.
   */
  @java.lang.Override
  public boolean hasHanji() {
    return ((bitField0_ & 0x00000001) != 0);
  }
  /**
   * <code>optional string hanji = 9;</code>
   * @return The hanji.
   */
  @java.lang.Override
  public java.lang.String getHanji() {
    return hanji_;
  }
  /**
   * <code>optional string hanji = 9;</code>
   * @return The bytes for hanji.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getHanjiBytes() {
    return com.google.protobuf.ByteString.copyFromUtf8(hanji_);
  }
  /**
   * <code>optional string hanji = 9;</code>
   * @param value The hanji to set.
   */
  private void setHanji(
      java.lang.String value) {
    java.lang.Class<?> valueClass = value.getClass();
  bitField0_ |= 0x00000001;
    hanji_ = value;
  }
  /**
   * <code>optional string hanji = 9;</code>
   */
  private void clearHanji() {
    bitField0_ = (bitField0_ & ~0x00000001);
    hanji_ = getDefaultInstance().getHanji();
  }
  /**
   * <code>optional string hanji = 9;</code>
   * @param value The bytes for hanji to set.
   */
  private void setHanjiBytes(
      com.google.protobuf.ByteString value) {
    checkByteStringIsUtf8(value);
    hanji_ = value.toStringUtf8();
    bitField0_ |= 0x00000001;
  }

  public static final int CANONICAL_TL_FIELD_NUMBER = 10;
  private java.lang.String canonicalTl_;
  /**
   * <pre>
   * v3.6.1 R2 — canonical TL romanization (identity sidechannel). Unlike
   * `roman` (field 8, the DISPLAY romanization — POJ-rendered in POJ mode,
   * recased per the typed segment), this stays the canonical TL the
   * `(hanji, canonical-TL)` word identity is keyed on (Core Principle #7):
   * `DictionaryRecord.tl` for dict-backed candidates, or
   * `phonetics::api::canonical_tl_form(native_roman, mode)` for
   * custom / walker-synth candidates. Snapshotted at candidate
   * construction, BEFORE the composing recase / POJ-render passes touch
   * `roman`. The platform round-trips it back via
   * `CommitContinuous.association_tl` so the NextWord association learns
   * the same TL a normal candidate commit would. Empty iff no canonical
   * TL is recoverable (TPS-OOV hanji-absent) — platform then omits
   * `association_tl` and the engine falls back to the raw committed slice.
   * NEVER consulted for the document commit (that goes through
   * `display_text`).
   * </pre>
   *
   * <code>string canonical_tl = 10;</code>
   * @return The canonicalTl.
   */
  @java.lang.Override
  public java.lang.String getCanonicalTl() {
    return canonicalTl_;
  }
  /**
   * <pre>
   * v3.6.1 R2 — canonical TL romanization (identity sidechannel). Unlike
   * `roman` (field 8, the DISPLAY romanization — POJ-rendered in POJ mode,
   * recased per the typed segment), this stays the canonical TL the
   * `(hanji, canonical-TL)` word identity is keyed on (Core Principle #7):
   * `DictionaryRecord.tl` for dict-backed candidates, or
   * `phonetics::api::canonical_tl_form(native_roman, mode)` for
   * custom / walker-synth candidates. Snapshotted at candidate
   * construction, BEFORE the composing recase / POJ-render passes touch
   * `roman`. The platform round-trips it back via
   * `CommitContinuous.association_tl` so the NextWord association learns
   * the same TL a normal candidate commit would. Empty iff no canonical
   * TL is recoverable (TPS-OOV hanji-absent) — platform then omits
   * `association_tl` and the engine falls back to the raw committed slice.
   * NEVER consulted for the document commit (that goes through
   * `display_text`).
   * </pre>
   *
   * <code>string canonical_tl = 10;</code>
   * @return The bytes for canonicalTl.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getCanonicalTlBytes() {
    return com.google.protobuf.ByteString.copyFromUtf8(canonicalTl_);
  }
  /**
   * <pre>
   * v3.6.1 R2 — canonical TL romanization (identity sidechannel). Unlike
   * `roman` (field 8, the DISPLAY romanization — POJ-rendered in POJ mode,
   * recased per the typed segment), this stays the canonical TL the
   * `(hanji, canonical-TL)` word identity is keyed on (Core Principle #7):
   * `DictionaryRecord.tl` for dict-backed candidates, or
   * `phonetics::api::canonical_tl_form(native_roman, mode)` for
   * custom / walker-synth candidates. Snapshotted at candidate
   * construction, BEFORE the composing recase / POJ-render passes touch
   * `roman`. The platform round-trips it back via
   * `CommitContinuous.association_tl` so the NextWord association learns
   * the same TL a normal candidate commit would. Empty iff no canonical
   * TL is recoverable (TPS-OOV hanji-absent) — platform then omits
   * `association_tl` and the engine falls back to the raw committed slice.
   * NEVER consulted for the document commit (that goes through
   * `display_text`).
   * </pre>
   *
   * <code>string canonical_tl = 10;</code>
   * @param value The canonicalTl to set.
   */
  private void setCanonicalTl(
      java.lang.String value) {
    java.lang.Class<?> valueClass = value.getClass();

    canonicalTl_ = value;
  }
  /**
   * <pre>
   * v3.6.1 R2 — canonical TL romanization (identity sidechannel). Unlike
   * `roman` (field 8, the DISPLAY romanization — POJ-rendered in POJ mode,
   * recased per the typed segment), this stays the canonical TL the
   * `(hanji, canonical-TL)` word identity is keyed on (Core Principle #7):
   * `DictionaryRecord.tl` for dict-backed candidates, or
   * `phonetics::api::canonical_tl_form(native_roman, mode)` for
   * custom / walker-synth candidates. Snapshotted at candidate
   * construction, BEFORE the composing recase / POJ-render passes touch
   * `roman`. The platform round-trips it back via
   * `CommitContinuous.association_tl` so the NextWord association learns
   * the same TL a normal candidate commit would. Empty iff no canonical
   * TL is recoverable (TPS-OOV hanji-absent) — platform then omits
   * `association_tl` and the engine falls back to the raw committed slice.
   * NEVER consulted for the document commit (that goes through
   * `display_text`).
   * </pre>
   *
   * <code>string canonical_tl = 10;</code>
   */
  private void clearCanonicalTl() {

    canonicalTl_ = getDefaultInstance().getCanonicalTl();
  }
  /**
   * <pre>
   * v3.6.1 R2 — canonical TL romanization (identity sidechannel). Unlike
   * `roman` (field 8, the DISPLAY romanization — POJ-rendered in POJ mode,
   * recased per the typed segment), this stays the canonical TL the
   * `(hanji, canonical-TL)` word identity is keyed on (Core Principle #7):
   * `DictionaryRecord.tl` for dict-backed candidates, or
   * `phonetics::api::canonical_tl_form(native_roman, mode)` for
   * custom / walker-synth candidates. Snapshotted at candidate
   * construction, BEFORE the composing recase / POJ-render passes touch
   * `roman`. The platform round-trips it back via
   * `CommitContinuous.association_tl` so the NextWord association learns
   * the same TL a normal candidate commit would. Empty iff no canonical
   * TL is recoverable (TPS-OOV hanji-absent) — platform then omits
   * `association_tl` and the engine falls back to the raw committed slice.
   * NEVER consulted for the document commit (that goes through
   * `display_text`).
   * </pre>
   *
   * <code>string canonical_tl = 10;</code>
   * @param value The bytes for canonicalTl to set.
   */
  private void setCanonicalTlBytes(
      com.google.protobuf.ByteString value) {
    checkByteStringIsUtf8(value);
    canonicalTl_ = value.toStringUtf8();

  }

  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, data);
  }
  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, data, extensionRegistry);
  }
  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, data);
  }
  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, data, extensionRegistry);
  }
  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, data);
  }
  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, data, extensionRegistry);
  }
  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, input);
  }
  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, input, extensionRegistry);
  }

  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return parseDelimitedFrom(DEFAULT_INSTANCE, input);
  }

  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return parseDelimitedFrom(DEFAULT_INSTANCE, input, extensionRegistry);
  }
  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, input);
  }
  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, input, extensionRegistry);
  }

  public static Builder newBuilder() {
    return (Builder) DEFAULT_INSTANCE.createBuilder();
  }
  public static Builder newBuilder(com.siansiansu.taigikeyboard.engine.proto.CandidateMessage prototype) {
    return DEFAULT_INSTANCE.createBuilder(prototype);
  }

  /**
   * <pre>
   * v3.5.8 Phase 6 — single span-local continuous candidate. Wire mirror of
   * `lexicon::RawCandidate` (`engine/lexicon/src/continuous.rs:84-124`).
   *
   * `consumed_span` is encoded as two scalar fields rather than a nested
   * message to keep wire overhead low. `consumed_span_start` /
   * `consumed_span_end` are byte offsets in the **original raw user
   * input** stored in `Phase::Continuous { raw }` — not in any canonical
   * TL transform. TL / POJ users → ASCII bytes; TPS users → Bopomofo
   * bytes (Phase 6 dispatcher converts TPS spans to canonical TL FST
   * keys for lookup but emits the span back in the user-facing TPS byte
   * space). Platform UI slices `pending[start..end]` off
   * `Phase::Continuous { raw }` (or its preedit mirror) on commit.
   *
   * `form` is currently always `1` (FORM_NOTONE; see
   * `engine/lexicon/src/continuous.rs:78`); reserved for hanzi (0) /
   * numeric (2) / abbrev (3) when proto-side carriers exist.
   *
   * `mode` (Phase 9.2) is the MOE-aligned candidate-type discriminator;
   * see `CandidateMode` above.
   *
   * v3.5.8 Phase 9 Item 5 — `roman` + `hanji` are display-only
   * sidechannels added so platform UI can build dual-line cells
   * (roman / hanji) the same way the legacy lexicon path does. The
   * engine continues to authority-stamp commit semantics and
   * `user_frequency.db` write keys via `display_text`
   * (= `hanji.unwrap_or(roman)`); `roman` and `hanji` are NEVER read
   * for commit. `roman` is the display romanization for the active
   * input mode — the underlying `DictionaryRecord.tl` by default, or
   * POJ-display (`oo`→`o͘`, `nn`→`ⁿ`, …) when
   * `AppConfig.input_mode = poj` (engine-rendered, mirroring the
   * dictionary-search path); `hanji` mirrors `DictionaryRecord.hanzi`
   * (proto3 `optional` distinguishes "TAILO candidate — no hanji
   * exists" from "wire-frame defect"). See
   * `docs/engine/continuous-candidate-display.md` §4 for the full
   * rationale and `docs/engine/continuous-input-ranking.md` §10.11
   * for the companion ranker dedupe rule.
   * </pre>
   *
   * Protobuf type {@code taigi.engine.CandidateMessage}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessageLite.Builder<
        com.siansiansu.taigikeyboard.engine.proto.CandidateMessage, Builder> implements
      // @@protoc_insertion_point(builder_implements:taigi.engine.CandidateMessage)
      com.siansiansu.taigikeyboard.engine.proto.CandidateMessageOrBuilder {
    // Construct using com.siansiansu.taigikeyboard.engine.proto.CandidateMessage.newBuilder()
    private Builder() {
      super(DEFAULT_INSTANCE);
    }


    /**
     * <code>uint32 consumed_span_start = 1;</code>
     * @return The consumedSpanStart.
     */
    @java.lang.Override
    public int getConsumedSpanStart() {
      return instance.getConsumedSpanStart();
    }
    /**
     * <code>uint32 consumed_span_start = 1;</code>
     * @param value The consumedSpanStart to set.
     * @return This builder for chaining.
     */
    public Builder setConsumedSpanStart(int value) {
      copyOnWrite();
      instance.setConsumedSpanStart(value);
      return this;
    }
    /**
     * <code>uint32 consumed_span_start = 1;</code>
     * @return This builder for chaining.
     */
    public Builder clearConsumedSpanStart() {
      copyOnWrite();
      instance.clearConsumedSpanStart();
      return this;
    }

    /**
     * <code>uint32 consumed_span_end = 2;</code>
     * @return The consumedSpanEnd.
     */
    @java.lang.Override
    public int getConsumedSpanEnd() {
      return instance.getConsumedSpanEnd();
    }
    /**
     * <code>uint32 consumed_span_end = 2;</code>
     * @param value The consumedSpanEnd to set.
     * @return This builder for chaining.
     */
    public Builder setConsumedSpanEnd(int value) {
      copyOnWrite();
      instance.setConsumedSpanEnd(value);
      return this;
    }
    /**
     * <code>uint32 consumed_span_end = 2;</code>
     * @return This builder for chaining.
     */
    public Builder clearConsumedSpanEnd() {
      copyOnWrite();
      instance.clearConsumedSpanEnd();
      return this;
    }

    /**
     * <code>uint32 syllable_count = 3;</code>
     * @return The syllableCount.
     */
    @java.lang.Override
    public int getSyllableCount() {
      return instance.getSyllableCount();
    }
    /**
     * <code>uint32 syllable_count = 3;</code>
     * @param value The syllableCount to set.
     * @return This builder for chaining.
     */
    public Builder setSyllableCount(int value) {
      copyOnWrite();
      instance.setSyllableCount(value);
      return this;
    }
    /**
     * <code>uint32 syllable_count = 3;</code>
     * @return This builder for chaining.
     */
    public Builder clearSyllableCount() {
      copyOnWrite();
      instance.clearSyllableCount();
      return this;
    }

    /**
     * <code>string display_text = 4;</code>
     * @return The displayText.
     */
    @java.lang.Override
    public java.lang.String getDisplayText() {
      return instance.getDisplayText();
    }
    /**
     * <code>string display_text = 4;</code>
     * @return The bytes for displayText.
     */
    @java.lang.Override
    public com.google.protobuf.ByteString
        getDisplayTextBytes() {
      return instance.getDisplayTextBytes();
    }
    /**
     * <code>string display_text = 4;</code>
     * @param value The displayText to set.
     * @return This builder for chaining.
     */
    public Builder setDisplayText(
        java.lang.String value) {
      copyOnWrite();
      instance.setDisplayText(value);
      return this;
    }
    /**
     * <code>string display_text = 4;</code>
     * @return This builder for chaining.
     */
    public Builder clearDisplayText() {
      copyOnWrite();
      instance.clearDisplayText();
      return this;
    }
    /**
     * <code>string display_text = 4;</code>
     * @param value The bytes for displayText to set.
     * @return This builder for chaining.
     */
    public Builder setDisplayTextBytes(
        com.google.protobuf.ByteString value) {
      copyOnWrite();
      instance.setDisplayTextBytes(value);
      return this;
    }

    /**
     * <code>float score = 5;</code>
     * @return The score.
     */
    @java.lang.Override
    public float getScore() {
      return instance.getScore();
    }
    /**
     * <code>float score = 5;</code>
     * @param value The score to set.
     * @return This builder for chaining.
     */
    public Builder setScore(float value) {
      copyOnWrite();
      instance.setScore(value);
      return this;
    }
    /**
     * <code>float score = 5;</code>
     * @return This builder for chaining.
     */
    public Builder clearScore() {
      copyOnWrite();
      instance.clearScore();
      return this;
    }

    /**
     * <code>uint32 form = 6;</code>
     * @return The form.
     */
    @java.lang.Override
    public int getForm() {
      return instance.getForm();
    }
    /**
     * <code>uint32 form = 6;</code>
     * @param value The form to set.
     * @return This builder for chaining.
     */
    public Builder setForm(int value) {
      copyOnWrite();
      instance.setForm(value);
      return this;
    }
    /**
     * <code>uint32 form = 6;</code>
     * @return This builder for chaining.
     */
    public Builder clearForm() {
      copyOnWrite();
      instance.clearForm();
      return this;
    }

    /**
     * <code>.taigi.engine.CandidateMode mode = 7;</code>
     * @return The enum numeric value on the wire for mode.
     */
    @java.lang.Override
    public int getModeValue() {
      return instance.getModeValue();
    }
    /**
     * <code>.taigi.engine.CandidateMode mode = 7;</code>
     * @param value The mode to set.
     * @return This builder for chaining.
     */
    public Builder setModeValue(int value) {
      copyOnWrite();
      instance.setModeValue(value);
      return this;
    }
    /**
     * <code>.taigi.engine.CandidateMode mode = 7;</code>
     * @return The mode.
     */
    @java.lang.Override
    public com.siansiansu.taigikeyboard.engine.proto.CandidateMode getMode() {
      return instance.getMode();
    }
    /**
     * <code>.taigi.engine.CandidateMode mode = 7;</code>
     * @param value The enum numeric value on the wire for mode to set.
     * @return This builder for chaining.
     */
    public Builder setMode(com.siansiansu.taigikeyboard.engine.proto.CandidateMode value) {
      copyOnWrite();
      instance.setMode(value);
      return this;
    }
    /**
     * <code>.taigi.engine.CandidateMode mode = 7;</code>
     * @return This builder for chaining.
     */
    public Builder clearMode() {
      copyOnWrite();
      instance.clearMode();
      return this;
    }

    /**
     * <code>string roman = 8;</code>
     * @return The roman.
     */
    @java.lang.Override
    public java.lang.String getRoman() {
      return instance.getRoman();
    }
    /**
     * <code>string roman = 8;</code>
     * @return The bytes for roman.
     */
    @java.lang.Override
    public com.google.protobuf.ByteString
        getRomanBytes() {
      return instance.getRomanBytes();
    }
    /**
     * <code>string roman = 8;</code>
     * @param value The roman to set.
     * @return This builder for chaining.
     */
    public Builder setRoman(
        java.lang.String value) {
      copyOnWrite();
      instance.setRoman(value);
      return this;
    }
    /**
     * <code>string roman = 8;</code>
     * @return This builder for chaining.
     */
    public Builder clearRoman() {
      copyOnWrite();
      instance.clearRoman();
      return this;
    }
    /**
     * <code>string roman = 8;</code>
     * @param value The bytes for roman to set.
     * @return This builder for chaining.
     */
    public Builder setRomanBytes(
        com.google.protobuf.ByteString value) {
      copyOnWrite();
      instance.setRomanBytes(value);
      return this;
    }

    /**
     * <code>optional string hanji = 9;</code>
     * @return Whether the hanji field is set.
     */
    @java.lang.Override
    public boolean hasHanji() {
      return instance.hasHanji();
    }
    /**
     * <code>optional string hanji = 9;</code>
     * @return The hanji.
     */
    @java.lang.Override
    public java.lang.String getHanji() {
      return instance.getHanji();
    }
    /**
     * <code>optional string hanji = 9;</code>
     * @return The bytes for hanji.
     */
    @java.lang.Override
    public com.google.protobuf.ByteString
        getHanjiBytes() {
      return instance.getHanjiBytes();
    }
    /**
     * <code>optional string hanji = 9;</code>
     * @param value The hanji to set.
     * @return This builder for chaining.
     */
    public Builder setHanji(
        java.lang.String value) {
      copyOnWrite();
      instance.setHanji(value);
      return this;
    }
    /**
     * <code>optional string hanji = 9;</code>
     * @return This builder for chaining.
     */
    public Builder clearHanji() {
      copyOnWrite();
      instance.clearHanji();
      return this;
    }
    /**
     * <code>optional string hanji = 9;</code>
     * @param value The bytes for hanji to set.
     * @return This builder for chaining.
     */
    public Builder setHanjiBytes(
        com.google.protobuf.ByteString value) {
      copyOnWrite();
      instance.setHanjiBytes(value);
      return this;
    }

    /**
     * <pre>
     * v3.6.1 R2 — canonical TL romanization (identity sidechannel). Unlike
     * `roman` (field 8, the DISPLAY romanization — POJ-rendered in POJ mode,
     * recased per the typed segment), this stays the canonical TL the
     * `(hanji, canonical-TL)` word identity is keyed on (Core Principle #7):
     * `DictionaryRecord.tl` for dict-backed candidates, or
     * `phonetics::api::canonical_tl_form(native_roman, mode)` for
     * custom / walker-synth candidates. Snapshotted at candidate
     * construction, BEFORE the composing recase / POJ-render passes touch
     * `roman`. The platform round-trips it back via
     * `CommitContinuous.association_tl` so the NextWord association learns
     * the same TL a normal candidate commit would. Empty iff no canonical
     * TL is recoverable (TPS-OOV hanji-absent) — platform then omits
     * `association_tl` and the engine falls back to the raw committed slice.
     * NEVER consulted for the document commit (that goes through
     * `display_text`).
     * </pre>
     *
     * <code>string canonical_tl = 10;</code>
     * @return The canonicalTl.
     */
    @java.lang.Override
    public java.lang.String getCanonicalTl() {
      return instance.getCanonicalTl();
    }
    /**
     * <pre>
     * v3.6.1 R2 — canonical TL romanization (identity sidechannel). Unlike
     * `roman` (field 8, the DISPLAY romanization — POJ-rendered in POJ mode,
     * recased per the typed segment), this stays the canonical TL the
     * `(hanji, canonical-TL)` word identity is keyed on (Core Principle #7):
     * `DictionaryRecord.tl` for dict-backed candidates, or
     * `phonetics::api::canonical_tl_form(native_roman, mode)` for
     * custom / walker-synth candidates. Snapshotted at candidate
     * construction, BEFORE the composing recase / POJ-render passes touch
     * `roman`. The platform round-trips it back via
     * `CommitContinuous.association_tl` so the NextWord association learns
     * the same TL a normal candidate commit would. Empty iff no canonical
     * TL is recoverable (TPS-OOV hanji-absent) — platform then omits
     * `association_tl` and the engine falls back to the raw committed slice.
     * NEVER consulted for the document commit (that goes through
     * `display_text`).
     * </pre>
     *
     * <code>string canonical_tl = 10;</code>
     * @return The bytes for canonicalTl.
     */
    @java.lang.Override
    public com.google.protobuf.ByteString
        getCanonicalTlBytes() {
      return instance.getCanonicalTlBytes();
    }
    /**
     * <pre>
     * v3.6.1 R2 — canonical TL romanization (identity sidechannel). Unlike
     * `roman` (field 8, the DISPLAY romanization — POJ-rendered in POJ mode,
     * recased per the typed segment), this stays the canonical TL the
     * `(hanji, canonical-TL)` word identity is keyed on (Core Principle #7):
     * `DictionaryRecord.tl` for dict-backed candidates, or
     * `phonetics::api::canonical_tl_form(native_roman, mode)` for
     * custom / walker-synth candidates. Snapshotted at candidate
     * construction, BEFORE the composing recase / POJ-render passes touch
     * `roman`. The platform round-trips it back via
     * `CommitContinuous.association_tl` so the NextWord association learns
     * the same TL a normal candidate commit would. Empty iff no canonical
     * TL is recoverable (TPS-OOV hanji-absent) — platform then omits
     * `association_tl` and the engine falls back to the raw committed slice.
     * NEVER consulted for the document commit (that goes through
     * `display_text`).
     * </pre>
     *
     * <code>string canonical_tl = 10;</code>
     * @param value The canonicalTl to set.
     * @return This builder for chaining.
     */
    public Builder setCanonicalTl(
        java.lang.String value) {
      copyOnWrite();
      instance.setCanonicalTl(value);
      return this;
    }
    /**
     * <pre>
     * v3.6.1 R2 — canonical TL romanization (identity sidechannel). Unlike
     * `roman` (field 8, the DISPLAY romanization — POJ-rendered in POJ mode,
     * recased per the typed segment), this stays the canonical TL the
     * `(hanji, canonical-TL)` word identity is keyed on (Core Principle #7):
     * `DictionaryRecord.tl` for dict-backed candidates, or
     * `phonetics::api::canonical_tl_form(native_roman, mode)` for
     * custom / walker-synth candidates. Snapshotted at candidate
     * construction, BEFORE the composing recase / POJ-render passes touch
     * `roman`. The platform round-trips it back via
     * `CommitContinuous.association_tl` so the NextWord association learns
     * the same TL a normal candidate commit would. Empty iff no canonical
     * TL is recoverable (TPS-OOV hanji-absent) — platform then omits
     * `association_tl` and the engine falls back to the raw committed slice.
     * NEVER consulted for the document commit (that goes through
     * `display_text`).
     * </pre>
     *
     * <code>string canonical_tl = 10;</code>
     * @return This builder for chaining.
     */
    public Builder clearCanonicalTl() {
      copyOnWrite();
      instance.clearCanonicalTl();
      return this;
    }
    /**
     * <pre>
     * v3.6.1 R2 — canonical TL romanization (identity sidechannel). Unlike
     * `roman` (field 8, the DISPLAY romanization — POJ-rendered in POJ mode,
     * recased per the typed segment), this stays the canonical TL the
     * `(hanji, canonical-TL)` word identity is keyed on (Core Principle #7):
     * `DictionaryRecord.tl` for dict-backed candidates, or
     * `phonetics::api::canonical_tl_form(native_roman, mode)` for
     * custom / walker-synth candidates. Snapshotted at candidate
     * construction, BEFORE the composing recase / POJ-render passes touch
     * `roman`. The platform round-trips it back via
     * `CommitContinuous.association_tl` so the NextWord association learns
     * the same TL a normal candidate commit would. Empty iff no canonical
     * TL is recoverable (TPS-OOV hanji-absent) — platform then omits
     * `association_tl` and the engine falls back to the raw committed slice.
     * NEVER consulted for the document commit (that goes through
     * `display_text`).
     * </pre>
     *
     * <code>string canonical_tl = 10;</code>
     * @param value The bytes for canonicalTl to set.
     * @return This builder for chaining.
     */
    public Builder setCanonicalTlBytes(
        com.google.protobuf.ByteString value) {
      copyOnWrite();
      instance.setCanonicalTlBytes(value);
      return this;
    }

    // @@protoc_insertion_point(builder_scope:taigi.engine.CandidateMessage)
  }
  @java.lang.Override
  @java.lang.SuppressWarnings({"unchecked", "fallthrough"})
  protected final java.lang.Object dynamicMethod(
      com.google.protobuf.GeneratedMessageLite.MethodToInvoke method,
      java.lang.Object arg0, java.lang.Object arg1) {
    switch (method) {
      case NEW_MUTABLE_INSTANCE: {
        return new com.siansiansu.taigikeyboard.engine.proto.CandidateMessage();
      }
      case NEW_BUILDER: {
        return new Builder();
      }
      case BUILD_MESSAGE_INFO: {
          java.lang.Object[] objects = new java.lang.Object[] {
            "bitField0_",
            "consumedSpanStart_",
            "consumedSpanEnd_",
            "syllableCount_",
            "displayText_",
            "score_",
            "form_",
            "mode_",
            "roman_",
            "hanji_",
            "canonicalTl_",
          };
          java.lang.String info =
              "\u0000\n\u0000\u0001\u0001\n\n\u0000\u0000\u0000\u0001\u000b\u0002\u000b\u0003\u000b" +
              "\u0004\u0208\u0005\u0001\u0006\u000b\u0007\f\b\u0208\t\u1208\u0000\n\u0208";
          return newMessageInfo(DEFAULT_INSTANCE, info, objects);
      }
      // fall through
      case GET_DEFAULT_INSTANCE: {
        return DEFAULT_INSTANCE;
      }
      case GET_PARSER: {
        com.google.protobuf.Parser<com.siansiansu.taigikeyboard.engine.proto.CandidateMessage> parser = PARSER;
        if (parser == null) {
          synchronized (com.siansiansu.taigikeyboard.engine.proto.CandidateMessage.class) {
            parser = PARSER;
            if (parser == null) {
              parser =
                  new DefaultInstanceBasedParser<com.siansiansu.taigikeyboard.engine.proto.CandidateMessage>(
                      DEFAULT_INSTANCE);
              PARSER = parser;
            }
          }
        }
        return parser;
    }
    case GET_MEMOIZED_IS_INITIALIZED: {
      return (byte) 1;
    }
    case SET_MEMOIZED_IS_INITIALIZED: {
      return null;
    }
    }
    throw new UnsupportedOperationException();
  }


  // @@protoc_insertion_point(class_scope:taigi.engine.CandidateMessage)
  private static final com.siansiansu.taigikeyboard.engine.proto.CandidateMessage DEFAULT_INSTANCE;
  static {
    CandidateMessage defaultInstance = new CandidateMessage();
    // New instances are implicitly immutable so no need to make
    // immutable.
    DEFAULT_INSTANCE = defaultInstance;
    com.google.protobuf.GeneratedMessageLite.registerDefaultInstance(
      CandidateMessage.class, defaultInstance);
  }

  public static com.siansiansu.taigikeyboard.engine.proto.CandidateMessage getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static volatile com.google.protobuf.Parser<CandidateMessage> PARSER;

  public static com.google.protobuf.Parser<CandidateMessage> parser() {
    return DEFAULT_INSTANCE.getParserForType();
  }
}
