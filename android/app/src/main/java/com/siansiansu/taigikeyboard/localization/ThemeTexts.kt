package com.siansiansu.taigikeyboard.localization

// Theme tab text constants (corresponds to iOS ThemeTexts.swift).
// P3 ships only the tab title; the editor / color-row strings arrive with the
// custom-theme editor (P4/P5). Built-in family titles live in BuiltInThemes.families
// (catalog display data), not here.
object ThemeTexts {
    const val tabTitle = "主題"
}
