// Compose compiler stability configuration.
//
// Declares types that the Compose compiler must treat as @Stable / @Immutable
// even when their structural form (e.g. holding a Map, List, or third-party
// type) would otherwise be inferred as unstable. One fully-qualified name per
// line. `//` comments and blank lines are ignored. `*` matches a single
// package segment; `**` matches any depth. See:
//
//   - https://developer.android.com/develop/ui/compose/performance/stability/fix
//   - https://kotlinlang.org/api/kotlin-gradle-plugin/compose-compiler-gradle-plugin/
//
// B10 step 1 (this slice) ships this file empty on purpose. Step 2 will be
// scoped after reading the compiler stability report — see
// `docs/perf/android-compose-stability.md` for the decision rule.
//
// Hot-path candidate-strip types under audit (do NOT add until proven):
//   - com.siansiansu.taigikeyboard.ime.text.smartbar.CandidateStripState
//   - com.siansiansu.taigikeyboard.ime.text.smartbar.CandidateMode
//   - com.siansiansu.taigikeyboard.ime.text.smartbar.CandidateDisplayParams
//   - com.siansiansu.taigikeyboard.ime.dictionary.TaigiWord  (holds Map)
