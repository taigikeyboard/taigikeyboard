# === macOS ===
.DS_Store
.AppleDouble
.LSOverride
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk
Icon

# === Windows ===
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# === Linux ===
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# === JetBrains IDEs ===
*.iml
*.ipr
*.iws
.idea/
!.idea/.name
!.idea/codeStyles/
!.idea/inspectionProfiles/

# === Python ===
__pycache__/
*.py[codz]
*$py.class
*.so
.Python
dist/
eggs/
*.egg-info/
*.egg
.venv
venv/
ENV/
.env
.envrc
.mypy_cache/
.pytest_cache/
.ruff_cache/

# === Gradle ===
.gradle/

# === Rust ===
/engine/target/
**/*.rs.bk
Cargo.lock.bak

# === D9.2 — Rust shared-core platform artifacts ===
# Android Rust .so output (regenerated by engine/scripts/build-android-libs.sh).
# Already covered by the global *.so rule above, but listed explicitly so the
# directory tree is obvious.
android/app/src/main/jniLibs/
# iOS xcframework + bridge .swift are committed under ios/RustEngine/ for
# stable Xcode source references — see plan v4 R3-H3 fix.

# === Claude Code ===
.claude/settings.local.json
.claude/projects/

references
!spec/references
