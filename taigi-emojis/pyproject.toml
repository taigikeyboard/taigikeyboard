[project]
name = "taigi-emojis"
version = "0.1.0"
description = "Single emoji source of truth for TaigiKeyboard (iOS + Android)"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest==9.1.1",
    "ruff==0.16.9",
]

[tool.ruff]
line-length = 110
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
