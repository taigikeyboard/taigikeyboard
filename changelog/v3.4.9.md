## v3.4.9

### iOS

#### Refactoring: Eliminate dictionary.db — Binary mmap Migration (48 MB → 12.7 MB, −73%)

Replaced SQLite `dictionary.db` with compact binary mmap files for all read-only dictionary data. SQLite retained only for user-writable databases.

**Before**: `dictionary.db` (44 MB) + `dictionary.trie` (4.1 MB) = 48.1 MB
**After**: `dictionary.trie` (5.2 MB) + `dictionary.bin` (4.4 MB) + `association.bin` (3.1 MB) = 12.7 MB

- **DictionaryBinaryReader**: mmap-based O(1) rowid→record lookup with offset table; 16-bit bitmask replaces 13 boolean SQL columns; `loadUnaligned` for safe unaligned integer decoding
- **AssociationBinaryReader**: mmap-based binary search on sorted key table for NextWord bigram predictions; replaces `word_association` SQLite table; bounds validation on all file-derived offsets
- **Hanzi search merged into dictionary.trie**: Hanzi prefix keys (`hanzi:好`) merged into main trie alongside romanization keys (`tl:`, `poj:`); eliminates separate `hanzi.trie` file and `hanziTrieService`
- **Multi-trie C bridge**: Handle-based API (`trie_create`/`trie_h_lookup`/`trie_h_prefix_search`/`trie_h_close`); legacy global trie API removed
- **TrieService**: Added `deinit` for handle cleanup; `LexiconConstants.TriePrefix.hanzi` constant for unified prefix pattern
- **DictionaryRepository**: Rewritten from SQLite to binary reader; extracted `lookupRowIds()` to eliminate duplicated trie lookup pattern; `EnabledDictionaries` with `sourceBitmask()` and `associationBitmask()`; named bitmask constants (`khiinBit`, `devBit`, `variantBit`)
- **NextWordService**: Dict association path migrated from SQLite to `AssociationBinaryReader`; preserves 2× over-fetch for dedup merging; bitmask construction centralized in `EnabledDictionaries`

#### New Files
- `Lexicon/Database/DictionaryBinaryReader.swift` — binary dictionary mmap reader
- `Lexicon/Database/AssociationBinaryReader.swift` — binary association mmap reader

#### Removed
- `Resources/Dictionaries/dictionary.db` — replaced by `dictionary.bin` + `association.bin`
- `Resources/Dictionaries/hanzi.trie` — merged into `dictionary.trie` with `hanzi:` prefix

### Android

#### Refactoring: Eliminate dictionary.db — Binary mmap Migration (48 MB → 12.7 MB, −75%)

Port of iOS binary mmap migration to Android. Same binary files, same data flow, Kotlin equivalents.

**Before**: `dictionary.db` (44 MB) + `dictionary.trie` (4.1 MB) = 48.1 MB
**After**: `dictionary.trie` (5.2 MB) + `dictionary.bin` (4.4 MB) + `association.bin` (3.1 MB) = 12.7 MB

- **DictionaryBinaryReader**: mmap via `FileChannel.map()` + `MappedByteBuffer` with absolute-position reads for thread safety; strict UTF-8 decoding matching iOS behavior; unsigned integer validation for header fields
- **AssociationBinaryReader**: binary search on sorted key table; strict UTF-8 decoding; pos synchronization on decode failure matching iOS byte-level behavior
- **EnabledDictionaries**: Extracted from `LexiconService` to shared file with `sourceBitmask()` and `associationBitmask()` matching iOS bit layout
- **LexiconService**: Rewritten from SQLite to `DictionaryBinaryReader`; `searchByHanzi()` changed from SQL `LIKE` to `hanzi:` trie prefix search (matches iOS)
- **NextWordService**: Dict association path migrated from SQLite to `AssociationBinaryReader`; preserves 2× over-fetch; bitmask filtering via shared `EnabledDictionaries`

#### New Files
- `dictionary/DictionaryBinaryReader.kt` — binary dictionary mmap reader
- `dictionary/AssociationBinaryReader.kt` — binary association mmap reader
- `dictionary/EnabledDictionaries.kt` — shared bitmask model

#### Removed
- `assets/dictionary.db` — replaced by `dictionary.bin` + `association.bin`

#### Refactoring: Scoring Pipeline Cleanup

- **ScoreBreakdown** data class: `calculateScore()` now returns a `ScoreBreakdown` instead of raw `Int` — eliminates duplicated scoring formula in `logScoreDetails()`
- **Per-candidate timestamp eliminated**: `sortByScore()` now computes `currentTime` once and passes it to `calculateScore()`
- **Redundant `withContext(Dispatchers.IO)` removed**: `sortByScore()` was wrapping in `Dispatchers.IO` but caller `search()` already runs on IO
- **Silent exception swallowing removed**: `sortByScore()` no longer catches and swallows exceptions — errors propagate to `search()` which has proper error handling
- **Candidate limit increased**: Search limit raised from 100 to 200 candidates
- **Trie lookup limits removed**: `TrieService.prefixSearch()` and `lookup()` no longer apply artificial result limits — buffer sized to actual key count

#### Refactoring: Main App Cleanup & Reorganization

- **Localization deduplication**: Extracted 14 shared `LocalizedText` entries (dictionary names, `cancel`, `viewWebsite`) from `Tab1Texts`/`Tab3Texts`/`Tab4Texts` into new `CommonTexts.kt`
- **Hard-coded strings localized**: Replaced English error strings (`"Export failed"`, `"Import failed"`, `"No email app found"`) with `LocalizedText` entries across 5 files
- **DiagnosticTexts merged**: 5 entries moved into `Tab4Texts` (prefixed `diagnostic*`); `DiagnosticTexts.kt` deleted
- **Folder restructure**: `ui/settings/` → `ui/tabs/` with `tab1/`–`tab4/` sub-packages; `diagnostics/DiagnosticService.kt` → `ui/tabs/tab4/`
- **Dead code removed**: Unused `subtypeManager` field, dead `onNewIntent`/`onDestroy` overrides, 5 unused `DictionaryInfoData` entries, unused imports
- **UI consistency**: `ArrowForward` → `KeyboardArrowRight` (4 places), inline `Text()` → shared `SectionHeader()` (2 places), `32.dp` → `AppStyle.sectionSpacing` (4 places)
- **Error handling**: Added error Toast for `resetAllSettings` failure (`Tab4Texts.resetFailed`)
- **DictionarySearchViewModel**: `PrefHelper` promoted from per-search creation to class field

#### Refactoring: Theme Unification & Style Centralization

- **Typography unified**: Merged `Type.kt` into `Theme.kt`; all font sizes now in M3 Typography (`MaterialTheme.typography.*`) with iOS-aligned sizes (body 18→17sp, sectionHeader 19→18sp, pageTitle 34sp, caption 14sp). Deleted `Type.kt`
- **AppStyle narrowed**: Removed 4 font size constants; AppStyle now holds only spacing, dimensions, and colors
- **Style constants centralized**: `AppStyle.trailingChevronSize` (24dp), `AppStyle.scrollContentBottomPadding` (40dp), `AppStyle.smallIconSize` (16dp), `AppStyle.selectionIconSize` (20dp)
- **Icon size tiers collapsed**: 3 tiers (16/17/20dp) → 2 tiers (16dp small, 20dp selection); trailing chevron unified to 24dp across all tabs
- **Hardcoded 14.sp eliminated**: Replaced with `MaterialTheme.typography.labelLarge` in 3 files
- **Style guide updated**: `rules/ui-style-guide.md` rewritten to reflect new architecture (Typography in Theme.kt, correct icon sizes, current AppStyle contents)

#### Refactoring: UI Component Review

- **Dead code removed**: Deleted unused `SegmentedButtonRow.kt` (0 references in codebase)
- **SettingInfoButton**: Parameterized hardcoded "OK" button via `dismissLabel` parameter (matches `ConfirmationDialog`/`ResultDialog` pattern)
- **CommonTexts.ok**: Added localized "好" entry for future UI localization
- **ColorRow, SliderRow**: Added missing `modifier: Modifier` parameter for composability

#### New Files
- `localization/CommonTexts.kt` — shared cross-tab localization entries

#### Refactoring: Tab1 Screens Review

- **Dead code cleanup**: Removed unused imports and variables
- **Extract `resolveDrawableResId` utility**: Shared drawable resource resolver replaces 6 inline implementations
- **Extract `FeatureList` composable**: Deduplicated feature list rendering from HomeScreen
- **Merge link cards**: `NavigationLinkCard`/`ExternalLinkCard` → single `LinkCard` composable
- **Content type constants**: Raw content type strings replaced with `ContentType` enum
- **Bug fix**: Fixed `SlideshowCard` index not resetting on image list change
- **Thread safety**: Added `@Volatile` to `FeatureContentLoader` cache fields
- **Split `DetailScreen`**: Separated into `DetailScreen` (UI) + `DetailItem` (model + builders)
- **Code quality**: Added file headers, removed "what" comments per ai-friendly-code rules

#### New Files
- `model/ContentType.kt` — content type constants
- `ui/tabs/tab1/DetailItem.kt` — detail item model and builders
- `util/ResourceUtils.kt` — shared drawable resource resolver

#### Refactoring: Remove Multi-Language Localization — Align with iOS

Removed the `LocalizedText` / `DisplayLanguage` / `LanguageManager` multi-language infrastructure. All `Tab*Texts` and `CommonTexts` converted from `LocalizedText(hanji = "...")` to plain `const val` String constants. Aligned with iOS which already uses plain `String` constants.

- **Deleted**: `LocalizedText.kt`, `DisplayLanguage.kt`, `LanguageManager.kt`
- **Tab*Texts / CommonTexts**: All entries converted to `const val` — compile-time inlined, zero runtime dispatch
- **Model layer**: `FeatureContent`, `CopyrightData`, `DetailItem` — `LocalizedText` → `String`
- **FeatureContentLoader**: JSON parser now extracts `getString("hanji")` directly (no `LocalizedText` construction)
- **UI composables**: `languageManager: LanguageManager` parameter removed from all composable functions; `currentLanguageFlow.collectAsState()` observations eliminated (removes unnecessary recomposition triggers)
- **Activity classes**: `LanguageManager.getInstance()` calls removed from all 8 Activity files; Toast messages use string constants directly
- **KeyView confirm key**: `DisplayLanguage`-based dispatch replaced with `Tab4Texts.confirmKeyLabel(inputMode, isTranslateSwapped)` — returns 選/soán/suán based on input mode
- **Deduplication**: `Tab3Texts.ok` removed, callers migrated to `CommonTexts.ok`
- **Smartbar**: `.hanji` property access on former `LocalizedText` objects replaced with direct constant references

#### Removed
- `localization/LocalizedText.kt` — multi-language wrapper data class
- `localization/DisplayLanguage.kt` — HANJI/POJ/TL enum
- `localization/LanguageManager.kt` — language switching singleton
- `localization/DiagnosticTexts.kt` — merged into `Tab4Texts`
- `diagnostics/` directory — `DiagnosticService.kt` moved to `ui/tabs/tab4/`
- `ui/theme/Type.kt` — merged into `Theme.kt`
- `ui/components/SegmentedButtonRow.kt` — unused dead code

### iOS

#### Refactoring: Localization Review (7-stage)

Complete review and simplification of `Localization/` module:

- **Dead code removed**: 19 unused `LocalizedText` properties deleted; `KeyboardTexts.swift` deleted (sole property unused)
- **Shared strings extracted**: 13 cross-tab duplicate strings consolidated into new `CommonTexts.swift` (aligned with Android `CommonTexts.kt`) — 9 dictionary names, 2 font names, `viewWebsite`, `cancel`
- **Same-file duplicates consolidated**: 3 identical import result format strings → single `importResultFormat`
- **Icon constants separated**: 7 SF Symbol icon names moved from `Tab4Texts` to new `SettingsIcons.swift` (aligned with Android `SettingsIcons.kt`)
- **Naming aligned**: `khpooDict`→`accentDict`, `openFontTitle`→`fontOpenHuninn`, `iansuiFontTitle`→`fontIansui`
- **Redundant comments removed**: Doc comments restating string literal values
- **`LocalizedText` wrapper removed**: `LocalizedText` struct and `LanguageManager` class deleted; all strings are now plain `String` constants; `@StateObject private var languageManager` removed from 18 View files; `.hanji` / `languageManager.text()` calls eliminated

#### New Files
- `Localization/CommonTexts.swift` — shared cross-tab string constants
- `Localization/SettingsIcons.swift` — SF Symbol icon constants for settings

#### Refactoring: Layout Review

- **TaigiLayouts**: Added `withGlobeKey()` helper; replaced 11 duplicated `_withGlobe` arrays with one-liner derivations (364→237 lines)
- **DeviceConfiguration**: Cached Touch ID detection as `static let` (hardware-fixed property); dropped unused `context` parameter
- **CustomLayoutService**: Renamed single-letter alias `A` → `layouts` for readability
- **KeyDef**: Condensed multi-line header per ai-friendly-code rules

#### Removed
- `Localization/KeyboardTexts.swift` — sole property unused
- `Localization/LocalizedText.swift` — `LocalizedText` struct + `LanguageManager` class + `withLanguageEnvironment()` extension

### Build Pipeline

- `dictionary/` — build pipeline producing binary format for both platforms (dictionary.db no longer deployed)
  - `10_create_dictionary_bin.py` — generates `dictionary.bin` with `--verify` mode
  - `11_create_association_bin.py` — generates `association.bin` with `--verify` mode
  - `04_create_trie.py` — generates merged `dictionary.trie` with `tl:`, `poj:`, `hanzi:` prefix keys
  - Struct format: `b` (signed) → `B` (unsigned) for all length fields
- **kesi → taigi-converter migration**: Replaced broken kesi dependency with taigi-converter (git submodule) via persistent Node.js subprocess bridge
  - `common/taigi_batch.js` — line-based JSON IPC server (convert, toToneNumber, validate)
  - `common/taigi_bridge.py` — Python wrapper with `convert_tl_to_poj`, `is_valid_romanization`, `normalize_taibun`
  - MOE 教育部造字碼 → Unicode mapping (9 code points) preserved from kesi
  - `parseSyllable` validation replaces `kam_haphuat` — strict TL phonological check

#### Refactoring: Scoring Pipeline Cleanup

- **ScoreBreakdown** struct: `calculateScore()` now returns a breakdown struct instead of raw `Int` — eliminates duplicated scoring formula in `logScoreDetails()` (was recalculating all 6 components for logging)
- **Per-candidate Date() eliminated**: `sortByScore()` now computes `currentTime` once and passes it to `calculateScore()` (was creating `Date()` per candidate)
- **`#if DEBUG` guard**: `logScoreDetails()` wrapped with `#if DEBUG` to match Android's `BuildConfig.DEBUG` guard — prevents unnecessary loop execution in release builds
- **TPS ㄜ expansion fix**: Moved TPS er/or expansion from `AutocompleteService` into `LexiconService.search()` so both result sets pass through `sortByScore()` together (was appending unsorted "or" results after sorted "er" results; now matches Android behavior)
- **Candidate limit increased**: Search and display limit raised from 100 to 200 candidates
- **Trie lookup limits removed**: `TrieService.prefixSearch()` and `lookup()` no longer apply artificial result limits — buffer sized to actual key count

#### Final Code Review Fixes (R1–R13)

4 independent reviewers (Claude, Codex, code-simplifier, refactor-reviewer) audited all 9 core files. 13 issues found and fixed:

- **Thread safety**: `TrieService` state access now serialized via `stateQueue` + `currentHandle`; `NextWordService` mutable state (`recordCounter`, `isUserTablesCreated`) protected by `NSLock`
- **Structural**: `EnabledDictionaries` extracted to `Lexicon/Models/EnabledDictionaries.swift`; `AssociationBinaryReader.passesFilter` API aligned to take `EnabledDictionaries` (matches `DictionaryBinaryReader`); invalid UTF-8 records now skipped instead of defaulting to `""`
- **Cleanup**: Removed dead `LexiconConstants.Database` (dictionary.db refs); `.map(\.self)` → `Array(...)`; duplicate `sqliteTransient` constant removed; `LexiconService` → `final class`; `TrieService.lookup()` magic 1000 → `Constants.defaultSearchLimit`; `word.last!` → `guard let`; stale comment updated; tautological `>= 0` checks on `UInt32` removed

#### New Files
- `Lexicon/Models/EnabledDictionaries.swift` — extracted from `DictionaryRepository.swift` (needs Xcode target addition)

### Shared

- Added `rules/ai-friendly-code.md` — naming, comments, and function design guidelines for AI readability
