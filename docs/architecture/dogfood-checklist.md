# Dogfood Checklist (real-device acceptance gate)

> **Type**: Reference (living)
> **Keywords**: `dogfood`, `acceptance`, `perf-gate`, `invariant`
> **Related**: behavioral-invariants.md, ../../.claude/rules/taigi-incidents.md

The qualitative perf gate (`~/.claude/rules/code-review-rules.md` §9) made concrete. Read this **when preparing a device dogfood pass or when a PR / memory entry cites an `Sn` item** — it is not always-on context.

- Base checklist: **S1 POJ diacritics**, **S2 TPS composition**, **S3 Hanji candidate scroll**, plus iOS keyboard-extension 64 MB hard cap, leak-free + no-keyboard-dismiss. Translates "perceptible regression on real interactive sequences" into a concrete acceptance gate.

Each **Sn** below = a dogfood acceptance item: **type X → expect Y** + the `INVARIANT_*` it pins. Full root-cause / fix-file / Codex-sandwich receipts live in `docs/architecture/behavioral-invariants.md §N` + the matching project-memory topic file (Claude auto-memory, `~/.claude/projects/<project>/memory/`) — not duplicated here. Recurring patterns (state once):

- **Evasion shape** (S4/S5/S9/S11/S13/S15/S17–S21): every hermetic fixture used a single reading / family / initial-form, so the production collision never fired in tests → real-device + the `engine/composing/tests/candidate_dump.rs` dev harness (production artifacts) are the catch-net. Android JVM can't load the `.so`/SQLite, so SQL-path items are dogfood-pinned there.
- **Fix-location lesson** (S5/§18, reused by S9/S17/S22): a display-only candidate-strip change belongs in the span-local key builder (`composing::shadow` / display-layer seam), never the shared segmentation primitive (`syllabifier::valid_span_endings`) — touching the primitive reintroduces the #290 non-greedy-recovery regression.
- **Build gate**: engine-touching items need `make build` (refresh xcframework/jniLibs) before device dogfood; `make dict` only when dict artifacts change. Platform-only items skip both.

Checklist (S7 intentionally absent):

- **S4 explicit-tone candidate filter** (2026-05-30 #367, §17): `tai5` → only tone-5 readings (NOT tai2/3/…); `tai` (no digit) → all tones (no-tone affordance).
  Status: shipped v3.6.0, never device-dogfooded
- **S5 longest-match prefix suppression** (2026-05-31 #371, §18): `tai5` → only 3-letter `tâi`, NOT 2-letter `ta` (乾/焦/大/…); `tai` (no digit) → same; bare short syllable `ka`/`m`/`ng` → that family still shows (it IS the longest single syllable); `taigikhipuann` → 台/台語/台語齒盤 (multi-syllable phrases stay).
  Status: shipped v3.6.0, never device-dogfooded
- **S6 first-candidate keycap-color hint** (visual, 2026-06-01 #373, §19): index-0 candidate in strip + expanded overlay + settings preview (`KeyboardPreviewPanel`) shows filled keycap-color bg (light=white / dark=keycap shade); others transparent. iOS+Android. Pure visual — this dogfood is the only gate.
  Status: pending
- **S8 leading khinsiann `--` literal** (2026-06-02 #379, §21): `--ah` → only `ah` underlined, `--` plain text (matches MOE); tap roman `ah`/Enter → `--ah`; tap Hanji 矣 → `--矣` (MOE keeps `--`). Internal hyphens unaffected: `tai-bak` hyphenated composes as one unit; `tsohjit` → `tso̍h--ji̍t`. iOS+Android engine fix.
  Status: shipped v3.6.1, never device-dogfooded
- **S9 continuous slot-0 respects dict separator** (2026-06-02 #380, §22): `hoogua` → [0] 予我 `hōo--guá` (dictionary form), NOT walker space-join `hōo guá`; 戶外 `hōo-guā` (tone 7) stays separate (#7). `taigi` → `tâi-gí` (hyphenated); `iasi` → `iā sī` (phrase); `taigikhipuann` → 台語齒盤 `tâi-gí khí-puânn` keeps space synthesis. iOS+Android engine fix.
  Status: shipped v3.6.1, never device-dogfooded
- **S10 auto-space attaching-punctuation swap** (2026-06-02 #381, §23): Auto-Space ON, commit a word → trailing space (`guá `); type `?`/`!`/`.`/`,`/`。` → space moves after symbol (`guá? `, consecutive `?!` → `?! `); opening brackets/quotes `(（[「『` keep LEADING space. Auto-Space OFF / TPS / swapped-Hanji → manual trailing space not eaten. iOS+Android platform-side, no engine change.
  Status: shipped v3.6.1, never device-dogfooded
- **S11 association cross-mode recall + no duplicate** (2026-06-03 #382, R1, §24): learn a bigram normally (canonical `prev_tl`), reach the same prev word via continuous input (raw `taigi` vs canonical `tâi-gí`) → learned next word still appears; reverse direction recalls; strip shows it once not twice; one character, several readings: 重/tāng vs 重/tàng both stay (#7). R1 = read-layer; write-side root = R2 (S12).
  Status: shipped v3.6.1, never device-dogfooded
- **S12 Continuous-input commit carries canonical TL (write-layer root fix)** (2026-06-03, R2, §25): learn a bigram BOTH via normal taps and continuous → both persist canonical `prev_tl`/`next_tl` (`tâi-gí`, NOT raw `taigi`) → predicts once, recall either path, no split rows in `user_association.db`. Verify POJ mode too (stores `tsiah` not `chiah`). iOS+Android.
  Status: pending
- **S13 Custom words findable across input modes** (2026-06-03 #384, R3, §26): add a custom entry in ONE mode → find from ALL — TL `tsiah` / POJ `chiah` / TPS Bopomofo, in keyboard strip AND settings browser (Tab3). Tone-aware: standalone-dot `U+02D9` matches stored combining `U+0307`. Pre-R3 entries still findable after migration backfill (iOS schema 1→2, Android DB 5→6). `(Hanji, romanization)` identity kept — two readings of same Hanji both stay.
  Status: shipped v3.6.1, never device-dogfooded
- **S14 Custom-word row-count cap parity** (2026-06-03, R4, §27): Android now enforces iOS's 30000-row cap. Import overflow → overflow rows reported skipped, existing grandfathered, no evict/crash; a file >30000 lines still rejected up-front. Manual add past cap → silent no-op (export-CSV is the escape hatch). Updating an existing entry always works. Android-only, no `make build`.
  Status: shipped v3.6.1, never device-dogfooded
- **S15 Polyphonic-character frequency (Hanji,canonical-TL) pair-key** (2026-06-04 #386, R5, §28): freq keyed by `(Hanji, canonical-TL)` pair (#7). Commit one reading (重/tāng 重量) → it climbs for ITS input, the other (重/tîng 重複) does NOT inherit the boost. Pre-R5 count sits in migrated `tl=''` legacy bucket, boosts until re-learned. v2 `.taigi` carries `tl` → both readings restore distinctly; pre-R5 v1 backup / hand-edited CSV still import to `tl=''`. Viewer (Tab3 frequency) shows one row/hanji (aggregated). iOS+Android.
  Status: pending
- **S16 user data excluded from OS auto-backup** (privacy, 2026-06-04 #388, R7, §29): three user DBs (frequency/association/custom words) excluded from OS auto-backup (iOS iCloud / Android Auto Backup + device-transfer). Typing / learning / ranking / dictionary-management browser unchanged. Manual `.taigi` export-reimport = the ONLY cross-device path. **Footgun**: reinstall / new device WITHOUT a manual export does NOT auto-restore (UI copy must never promise iCloud restore). iOS sets `isExcludedFromBackup` on each DB open (backfills legacy installs); Android already `allowBackup="false"`. iOS code + Android doc-only, no `make build`. Product decision (exclude all three): USER 2026-06-04.
  Status: pending
- **S17 Literal composing — TL+POJ, no spelling conversion** (2026-06-05 #390, §30): in BOTH TL/POJ, composing display (preedit + Enter/Space commit) = typed letters, only tone digit → mark, NO spelling-family fold. TL `teng2` → `téng` (NOT `tíng`); `goa2` → `goá`; `ting2` → `tíng`. POJ `ting2` → `tíng` (TL spelling kept); `goa2` → `góa`; `chiah8` → `chia̍h`. frequency + association STAY cross-mode shared (identity keys canonical, separate from display). Invalid `xyz2` / unhyphenated `goa2ai3li2` verbatim (no auto-syllabify, §10.2). iOS+Android, no `make dict`. ⚠ follow-up gap: a POJ continuous OOV-synth candidate **tapped** still recases (`recase_tl_as_poj_display`) — entangled with S9/S13 shared recase, deferred.
  Status: shipped v3.6.1, never device-dogfooded
- **S18 TPS space key = soft syllable separator (tone-1 continuous input)** (2026-06-05 #391, §31): TPS space = tone-1 / boundary marker. `ㄍㄠ`␣`ㄉㄞ` → 交代 (kau-tài) spanning whole preedit (space consumed, no dangling); `ㄉㄞ`␣`ㄍㄧ` → 台機. Non-first-tone already delimits. TL/POJ space stays a literal word boundary (`tai uan` ≠ one `taiuan`). Entering-tone `kat` (ㄍㄚㆵ) intact. iOS+Android engine, no `make dict`. ⚠ no-space `ㄍㄠㄉㄞ` is the separate S19/S20 path.
  Status: pending
- **S19 TPS stop-coda gate — tone 1 without space** (2026-06-05 #392, §32): `ㄍㄠ``ㄉ``ㄞ` (no space) → 交代; pre-fix `ㄉ`→ㆵ folded to non-syllable `kaut`. Gate: fold a dual-form STOP to coda only when `is_valid_syllable(tps_to_tl(pending+coda))`. Still fold: `kat`/`kap`/`kak`/`op`/`ok`/`kuat`/`kuak`/`auh`. Keep initial: `kaut`/`ot`/`kiaut`/`kuap`. STOPS only (ㄅㄉㄍㄏ) — nasals = S20. iOS+Android engine, no `make dict`.
  Status: shipped v3.6.1, never device-dogfooded
- **S20 TPS nasal-coda gate — tone 1 without space** (2026-06-06 #394, §33): S19 sibling for nasals ㄇ/ㄋ/ㄫ. 龜毛 `ㄍㄨ``ㄇ``ㆦ``ˊ` (no space) → 龜毛 (ku-môo); pre-fix `ㄇ`→ㆬ folded to non-syllable `kum`. Still fold valid coda: am/an/ang/ing/kun. Keep initial: kum/aum/aun/aung/kuing. Same gate, helper generalized `stop_coda_form` → `dual_final_form`. iOS+Android engine, no `make dict`. C-class (op/ok/auh valid → ambiguous 烏白/包袱) + whole-word-vs-single-char ranking gap remain out of scope (see S21).
  Status: shipped v3.6.1, never device-dogfooded
- **S21 TPS de-fold enumerate — hidden words surface as candidates** (2026-06-06 #396, §35): a word hidden because a **valid** coda was folded but is really the next syllable's onset now surfaces. No space: 雞胸 `ㄍㆤㆷㄧㄥ` → 雞胸 (ke-hing); 烏白 `ㆦㆴㆤㆷ` → 烏白; 包袱 `ㄅㄠㆷㆦㆻ` → 包袱. **Acceptance = word appears in strip, NOT necessarily #0** (de-fold is span-local, base lattice owns slot-0). Regression: 交代 / 結 unchanged; candidate count bounded. Separator guard: `ㄍㆤㆷ`␣`ㄧㄥ` (space) → NOT de-folded. Fix = a 2nd "de-fold" shadow (one coda glyph → onset, byte-preserving), librime Spelling-Algebra "alternate spelling = extra path". iOS+Android engine, no `make dict`. ⚠ ranking-cohesion follow-up (non-word 到台/到好 outranks whole word; McBopomofo epsilon-boost) unscoped.
  Status: shipped v3.6.1, never device-dogfooded
- **S22 Literal romanization candidate — fast Hanji-with-romanization input** (2026-06-06 #395 toned-cut → #397 always-show, §34): in TL/POJ, **whenever composing** (tone OR not) the strip surfaces at index 0 a roman-only candidate = the preedit literal: `tai` → `tai`, `nng7` → `nn̄g`, `taigi` → `taigi`, `tai5-gi2` → `tâi-gí`. So Hanji-with-romanization writing commits romanization in ONE tap with no `文/A` toggle — even in Hanji (swapped) mode (PhahTaigi parity) — and the strip doesn't jump as you type past a tone. Mirrors preedit verbatim (`tai1` / `goa2ai3li2` raw digits, no auto-syllabify §10.2). Excludes TPS / English / empty preedit. Dict/walker best candidate moves to #1 (expected). Identity #7 untouched (learns canonical `guá`). iOS+Android engine-only (platforms already commit a hanji-absent candidate's `roman` in swapped mode), no `make dict`. #395 shipped toned-only → #397 relaxed to always-show after USER: "the logic should be consistent". **Show Typed Text toggle on all four platforms, default on (2026-09-03, USER: "all four platforms get the 'Show Typed Text' toggle, with the default switched to on everywhere")**: iOS/Android in the host-app Typing settings + the in-keyboard overlay; macOS/Windows in the General pane, **placed right below Auto-Space**. i18n label renamed from Show Original Romanization Candidate to Show Typed Text. Only its default moved: #398 ON → #445 mobile OFF (2026-06-19) → #669 desktop always on, no toggle (2026-09-02) → 2026-09-03 toggle on all four platforms, default ON. **A stored value always beats the default**; users who turned it off stay off after upgrading. Desktop behaviour (when on): TL/POJ type `taigi` → candidate window slot 0 = the `taigi` literal cell (no Hanji), `台語` moves to the second cell (`w` / →); **Enter commits `taigi` directly**; ⇧Enter is still the raw commit; Space on the literal cell is `.ignored`; in romanization mode the literal and the same-roman dictionary row merge into one cell kept at slot 0; under Hanji with Romanization the literal cell comes first, then the first Hanji cell. When off, slot 0 returns to the dictionary cell with Hanji. ⚠ The `literalRomanCandidateEnabled` tombstone in macOS `RetiredSettingsCleanup` **must be removed** — in the #558 era that row became a dead toggle because this launch sweep cleared it on every launch; the `toggleLiteralRomanCandidate` shortcut stays retired (this round restores only the settings row, not the hotkey). Windows has no retired-key sweep. ⚠ Known pre-existing gap (unfixed): under Hanji-first side-by-side / Hanji with Romanization, Enter committing the literal **may not add auto-space** (`AutoSpacePolicy` decides by script+swap, and the literal cell is `.primary`); romanization-first / romanization mode do add the space. Wiring on all four platforms, engine untouched, no `make build`. Carrier = `FetchAtPos.literal_roman_candidate_disabled` (field 6, **inverted** so proto3-default false = show = legacy, mirrors `enabled_sources_bitmask` sentinel); platform setting positive on all four (`isLiteralRomanCandidateEnabled` / `literalRomanCandidateEnabled` / `is_literal_roman_candidate_enabled`, default TRUE since 2026-09-03), mobile `ComposingManager` / desktop bridge inverts. Gates ONLY the §34 forced prepend, not natural roman candidates. ⚠ **caught 3 pre-existing main test failures from #397** (always-show literal slipped onto main with #397's test gate skipped): `dispatch_continuous::{decode_fetch_at_pos_continuous_lexicon_unavailable_returns_empty_carrier, fetch_at_pos_carries_user_freq_snapshot_through_decode}` + `continuous_slot0_dict_roman::slot0_promotes_dict_khinsiann_form_over_space_synth` — all asserted empty/dict-slot-0 but the literal now occupied index 0. Fixed by setting `disabled=true` in those lexicon/dict-charter fixtures (the literal is orthogonal noise there) — restores their intent + doubles as OFF-gate coverage.
  Status: shipped v3.6.1, never device-dogfooded
- **S23 TPS ambiguity-aware lookup — one-key-many-glyph ambiguity is deferred to the dictionary** (2026-08-19, §35 full rewrite; replaces the three-generator version of the same number): type with no tone, no space — 考卷 `ㄎㄛ`+`ㄍ`+`ㄫ` → 考卷; 毋是 `ㄇ`+`ㄒ`+`ㄧ` → 毋是; **會記得** `ㆤ`+`ㄍ`+`ㄧ`+`ㄉ`+`ㄧ`+`ㄉ` → 會記得 (multiple substitution points, which the old three generators could not reach); **毋好** `ㄇ`+`ㄏ`+`ㄛ` → 毋好 (A+B mixed form); 啥物 `ㄒㄧㄇㄇㄧㄏ` → 啥物; 今仔日 / 遮爾 likewise. **A lone `ㄇ` → 毋 appears directly; a lone `ㄫ` → 黃/向** (no long-press needed). ⚠ The second half — "multi-syllable words starting m- / ng- still listed" — is superseded by **S25/§43**: one bare zhuyin key = one syllable, single-syllable candidates only. The composing area still shows the auto-corrected literal (`ㄎㄛㆻㄫ`) — disambiguation happens at the lookup layer, the raw buffer is untouched. Negative controls: 門 `ㄇㆭ` / 兩 `ㄋㆭ` / 博雅 / 名 `ㄇㄧㄚ` / 龜毛 / 交代 / 雞胸 / 烏白 / 包袱 must not disappear; `ㄎㄛㆻ`␣`ㄫ` (space in between) must **not** produce 考卷, but `ㄇ`␣`ㄒㄧ` **must** produce 毋是 (directional rule); `ㆬㄚ` (long-press ㆬ) → 毋仔 must appear (嘛/媽 ranking first by frequency is expected; no-penalty decision). ⚠ **Expect fewer candidates**: inputs like `ㄍㆤㆷㄧㄥ` lose many short-prefix single characters (`ke` / `pau` / `oo`) — the old de-fold second lattice bypassed the §18 longest-match suppression; that inflation is now fixed, not a regression. Engine-only, needs `make build`. macOS has no TPS, unaffected. Custom dictionary not in this round (platform pre-lookup architecture).
  Status: shipped v3.6.5, never device-dogfooded
- **S24 TPS space pins the unmarked tones (tone 1 / tone 4)** (2026-08-20, §41): TPS layout, type `ㄒㄧ` + **space** → tone 1 only (詩/司/絲/思/施), **must not** also show 是 (si7) / 時 (si5) / 死 (si2); type `ㄐㄧㆵ` + space → tone 4 only (這/即/今), **must not** show 一/蜀 (tsit8, 9× the frequency); `ㄏㆦ` + space → hoo1 only (呼), no 予 (hoo7). **No space = unchanged behaviour**: `ㄒㄧ` / `ㄐㄧㆵ` alone still show every tone (toneless-input affordance). A tail that already carries a tone mark is unaffected: `ㄒㄧˋ` → 死, `ㄍㄠ˪` + space → 到 (kau3) still there. Negative controls (must not disappear): `ㄍㄠ`␣`ㄉㄞ` → 交代, `ㄉㄞ`␣`ㄍㄧ` → 台機 (§31 whole word across the space; first syllables kau1 / tai5 carry different tones), `ㄇ`␣`ㄒㄧ` → 毋是 (§35; first syllable m7), `ㄎㄛㆻ`␣`ㄫ` still must **not** produce 考卷. Custom words are pinned the same way: add a custom word with a tone-7 reading (e.g. `sī`), type its toneless keys `ㄒㄧ` + space → that word **must not** appear; without the space it appears as before (the toneless affordance also holds for custom words); a custom tone-1 word (e.g. `sai`) typed as `ㄙㄞ` + space → **must** appear. **The space marker is invisible and is never committed** (2026-08-21 dogfood addendum): after the space the composing area shows `ㄒㄧ`, **not** `ㄒㄧ␣`; for `ㄍㄠ`␣`ㄉㄞ`␣ the composing area shows `ㄍㄠㄉㄞ` and Enter commits `ㄍㄠㄉㄞ` (no spaces; the romanization NextWord learns has none either). After picking a candidate that covers the whole span no composing state may remain (the next backspace deletes a real character, not the invisible marker). TL/POJ space stays a literal space, unaffected. Engine-only, needs `make build` (**not** `make dict`). macOS has no TPS, unaffected.
  Status: pending
- **S25 A candidate may not have more syllables than the input** (2026-08-21, §43): all three platforms. TL `kesi` → 家私頭仔 (ke-si-thâu-á) **must not** appear; `kesithau` → 家私頭 **must** be there, 家私頭仔 **must not**; `tsuisi` → 水手 **must** be there, 水社寮 / 水仙宮 **must not**. Boundary immediacy: `tai` → 台語 **must not** appear; `taig` → 台語 **must** appear; `taigi` → 台語 (including the `tâi-gír` extension) as before. Same with tones: `tai5` → single syllables only (台/臺/抬…), **no** 台灣 / 台語 / 台灣人; `tai5gi2` → 台語 **must** be there, 台語文 **must not**. POJ: same readings, same results (`chuisi` / `goa2`). TPS: `ㄍㄠ` → single syllables only (交/到/教…), **no** 交代; `ㄍㄠㄉ` → 交代 **must** come back. ⚠ **Expected change** (not a regression): a lone initial / zhuyin key (`t` / `s` / `ㄍ` / `ㄇ`) yields single-syllable characters only, multi-syllable words are no longer listed — this replaces the second half of S23's "a lone `ㄇ` still lists m- words" (the first half, 毋 appearing, still holds). Negative controls (must not disappear): all of §35 — 考卷 `ㄎㄛㆻㄫ`, 毋是 `ㄇㄒㄧ`, 會記得, 雞胸, 烏白, 包袱; §31 `ㄍㄠ`␣`ㄉㄞ` → 交代; §18 `tai5` → only `tâi`, no `ta`; §22 `hoogua` → 予我 at slot 0; §34 the literal roman candidate stays at index 0; an exact hit `aia` → 阿依仔 / 阿姨 still there (the whole word was typed). **Custom words are exempt**: a 3-syllable custom word must still appear when only its prefix is typed (PO decision 2026-08-21). Engine-only, needs `make build` (**not** `make dict`).
  Status: shipped v3.6.5, never device-dogfooded
- **S26 Candidate display = Romanization Only** (2026-09-01, §44; supersedes the USER-approved exception in §42's core sentence): all four platforms. Settings → Candidate Display → Romanization Only (default Hanji–Romanization Pairing = today's behaviour, unchanged to the character). TL `taigi` → candidate cells carry **romanization only** (`tâi-gí`, `tâi`, …), no subtitle; strip scrolling / expansion / S6 index-0 keycap / toolbar **all unchanged**. Toneless affordance: `si` → sī / sî / sí / si … one cell each; `hoogua` → `hōo--guá` at slot 0 (S9). **Homophone collapse**: `tsiah` → 食/𤆬 `tsia̍h` collapse into **one cell** (two rows in side-by-side mode); 重 tîng / tāng → **two cells** (different romanization, #7); the §34 literal `tsiah` and the dictionary `隻/tsiah` → **one cell**, the literal keeps slot 0 (two `tsiah` cells = dedupe at the wrong layer); since 2026-09-03 the dedupe key is **the rendered romanization alone**, so the same string with a different consumed span also collapses into one cell. **NextWord**: in side-by-side mode learn `guá → 食` and `guá → 𤆬`, switch to roman-only and commit `guá` → the prediction row shows **one cell** `tsia̍h`; switch back → two entries with Hanji. Tap → the document receives romanization; auto-space + S10 fire per the roman-ish rule; POJ shows POJ (`goá`); garbage `xyz2` → only the literal remains, preedit unchanged (§10.2). Frequency / association rows = same as today's roman-first commit (inspect in Tab3; switching back to side-by-side, the same row rises); tapping a collapsed group learns the survivor's identity (when the literal is in the group = the roman bucket). **The 文/A key**: mobile **hidden** (neither the bottom-row key nor the expanded-overlay button appears; the space bar absorbs the width; after the host app changes the mode, the key is still absent the next time the keyboard shows; USER 2026-09-12: "no need to show it … it frees up a bit of space" — the earlier "visible but inert" is void); macOS / Windows `` ` `` shortcut is a silent no-op; **punctuation is always half-width** (`,` `.` `?` on the character + symbol pages, host receives `,` on desktop) whatever the stored swap says; switching back to side-by-side → the original swap state is **restored** (the stored value must not be overwritten); the **Annotate in Brackets** toggle is disabled in both the host app and the in-keyboard overlay, shows the stored value, and committed text gains no brackets. macOS: all three layouts roman-only; **Space on a roman-only cell = `.ignored`** (existing #610 path, must be pinned); full-width punctuation off; **the candidate window keeps one row of height** (when no cell carries an annotation no subtitle space is reserved — 2026-09-02 dogfood addendum; switching back to side-by-side, or a cell with Hanji appearing in the row, brings two-row height back). Windows: candidate window roman-only, same one-row height; Space `Ignored` as on macOS. Choosing Romanization Only on the TPS layout → TPS behaviour unchanged (Hanji cells, S23–S25 unaffected); English unaffected; switching back to TL → the mode is restored. Side-by-side ↔ roman-only switches live (host / overlay / pane): the strip redraws immediately, no stale layout, no width jump or crash. Custom words: a custom `tsia̍h/X` homophonous with a dictionary entry **may** be collapsed behind 食 (accepted in round 1, §44). Engine + four platforms, needs `make build` (**not** `make dict`).
  Status: pending
- **S27 Candidate display = Hanji with Romanization (Hanji and roman mixed)** (2026-09-01 single-label version → **corrected 2026-09-02: split-cell version**, §42 second exception): Settings → Candidate Display → Hanji with Romanization. **Desktop (macOS / Windows, #666)**: TL `taigi` → the candidate window lists a `台語` cell, a `tâi-gí` cell, a `台` cell, a `tâi` cell, … — **Hanji and romanization each in their own cell, adjacent, neither with a subtitle**, **not** one `台語 tâi-gí` label; the whole row is **one line high**. Enter / slot key on `台語` → the document receives 台語 (no auto-space; with Annotate in Brackets ON, `台語 (tâi-gí)`); on `tâi-gí` → the document receives tâi-gí (**with** auto-space); Space on the `台語` cell → tâi-gí, on the `tâi-gí` cell → 台語. Homophones: `tsiah` → `食 tsia̍h 𤆬` (**one** `tsia̍h` cell, right after 食; 𤆬's roman cell is collapsed); the §34 literal `tsiah` at slot 0 absorbs the dictionary `隻` roman cell → `tsiah 隻 食 tsia̍h 𤆬`; 重 tîng / tāng → `重 tîng tāng` **three cells** (2026-09-03: same-Hanji cells collapse into one, both roman cells stay — the losing reading is still reachable through its own roman cell). **The 文/A key (corrected 2026-09-13, USER: "Hanji with Romanization needs the isTranslateSwapped button, because switching between full-width and half-width punctuation is still needed")**: mobile **shown** (bottom row + expanded overlay) and desktop `` ` `` **live** — under Hanji with Romanization it flips **only the punctuation width**: tap/press → the character + symbol pages switch `,` `.` `?` ↔ `，` `。` `？` (Android also the `、` slot on the symbol page; desktop host receives `，` for `,`), the bottom-row key lights when full-width (iOS filled icon / Android active fill; the Android overlay button too — the iOS overlay button only has press feedback), the candidate cells do **not** change (still split Hanji / romanization, hanji first); tap again → half-width. Opening Hanji with Romanization with the swap already stored ON (from side-by-side hanji-first) → full-width from the start. **TPS (both mobile platforms)**: the symbol pages (`?123` / `#+=`) are full-width from the start whatever the swap was, the Android `、` slot is present on the symbol page, no 文/A on any page (bottom row + overlay); leaving TPS → the pages go back to the stored width. Switching back to side-by-side → title+subtitle, and the swap state is whatever the last tap left (the same stored flag); cycling ⌃⌘H / Ctrl+Alt+H onto Hanji with Romanization flashes it and an open row re-lays out in place into split cells. POJ mode renders POJ in the roman cells. **Mobile (iOS / Android, #668)**: strip / expanded overlay split the same way — `tsiah` → `tsiah 隻 食 tsia̍h 𤆬` one cell each, no subtitle; tapping the `tâi-gí` cell → the document receives tâi-gí (**with** auto-space, no brackets under Annotate in Brackets); tapping the `台語` cell → 台語 (Annotate in Brackets ON → `台語 (tâi-gí)`); frequency / association learn the same `(Hanji, canonical-TL)` identity from both cells (the same row rises in Tab3). **Under Romanization Only / Hanji with Romanization the strip and expanded cells reserve no blank second line** (iOS removes the invisible subtitle row; Android switches the title font size to a single-line configuration filling the strip height); side-by-side rows (even with one or two pure-roman cells mixed in) still align on two lines. The NextWord prediction row **also splits since 2026-09-12** (see S44). Mobile has no Space-commits-the-other-script (platform difference, desktop-only); **the Hanji/roman flip (Space) keeps its original behaviour under Hanji with Romanization / Romanization Only** (USER 2026-09-03: "I don't want to disturb established habits"). **Dedupe rule from 2026-09-03 (four platforms)**: under Hanji with Romanization the Hanji cells and the roman cells are deduped **separately** by "the text shown on the cell", first wins — a cell that reads the same as one already on screen never appears twice (the consumed span is not part of the key); side-by-side mode is untouched (the subtitle distinguishes 重/tîng from 重/tāng). The engine only changes the §44 roman dedupe key (span removed); the Hanji with Romanization split stays platform-side; needs `make build` (**not** `make dict`).
  Status: pending
- **S28 Nasal rime `o͘ⁿ` alias spellings + double-tap nn/oo** (2026-09-04 #685/#686, §45): POJ keyboard `o͘` key + `nn` key (or plain ASCII) — `ho͘nn` / `ho͘nnh` / `ho͘ⁿ` / `ho͘ⁿh` / `hoonn` / `hoonnh` — **all six spellings** must produce 好/否/呼/齁/乎/歟 at slot 1+; multi-syllable 好玄 `hoonnhian`, 呼呼 `hoonnhoonn`; scrolling down on a partial `hoonnh` must show 好玄 / 好賢 / 呼呼 / 鼾鼾. **Negative controls (must not disappear)**: 滷卵 `lo͘nng` + `loonng`, 芋卵 `oonng`, 菜脯卵 `chhaipoonng`, 苦惡鳥 `khooonntsiau`, 可惡 `khooⁿ` + `khoonn` — these five were already broken under TL **glyph** input (whole-span fold mis-folded across syllables) and are fixed in the same round. **Unchanged controls**: candidate counts for `hoo` / `hoon` / `koo` / `sioo` identical to main (prefixes of the alias face must not be polluted; 護欄 / 虎貓 / 好學 must still be there). **Double-tap**: with both toggles on, `hoonn` → composing area `ho͘ⁿ` (not `ho͘nn`), and the committed text is `ho͘ⁿ` as well; case: `HOONN` → `HO͘ᴺ`, `hoO` → `ho͘`; `OOoo` → `O͘o͘` (no stacked combining dots). ⚠ **Accepted ambiguity**: `khoonn` / `khooonn` keep both readings (可惡 and 可/坷/考); the extras are expected. ⚠ **Custom words**: a stored word with an `onn` reading is reachable through the `o͘ⁿ` spelling — verified in the engine, not reported item by item on device. Engine + dictionary build, needs `make dict` **and** `make build`, no platform code. USER dogfood PASS 2026-09-04 (six spellings + double-tap).
  Status: PASS 2026-09-04

- **S29 Windows app name follows the system locale** (2026-09-05, parity with macOS `CFBundleDisplayName`): Windows. After install, **Settings > Time & language > Language & region > Chinese (Traditional, Taiwan) options > Keyboards > installed keyboards / add a keyboard** shows **台語齒盤**, **not** a path like `@C:\Program Files\TaigiKeyboard\TaigiKeyboard.dll,-100` (the original bug). The same IME's **Start menu** entry and its row under **Settings > Apps > Installed apps** also show 台語齒盤. Switch the system display language to English / Japanese (sign out and back in) → all three become `TaigiKeyboard` / `台湾語キーボード`; switch back → 台語齒盤. ⚠ The shell caches names — **sign out and back in** (or reboot) to see it; reopening Settings is not enough. Negative controls (unchanged): the language bar / tray button was already localized via `LoadStringW`; after an **upgrade** (new build installed over the old) the uninstall row is still found (AppId unchanged); after uninstall `C:\ProgramData\Microsoft\Windows\Start Menu\Programs\desktop.ini` contains **no** leftover `TaigiKeyboard.lnk=` line; `regsvr32 /u` is still clean (the profile disappears). Verification (dev box): `reg query "HKLM\SOFTWARE\Microsoft\CTF\TIP\{32C28A51-8939-4C8F-8F29-037F9FD3CF0A}\LanguageProfile" /s` → `Description = TaigiKeyboard` (plain string) + `Display Description = @<dll path>,-100`. Windows-only, no `make build` / `make dict` (engine untouched), but **requires reinstall** (TSF registration and installer both changed).
  Status: PASS 2026-09-04 (per project memory)

- **S30 macOS font-management pane + custom font upload** (2026-09-08, roadmap § Desktop custom fonts): macOS. The settings window sidebar gains a sixth row **Manage Typefaces** (**last**, below Manage Dictionaries, icon `textformat`); the row label fits, untruncated, in all five display languages. ⚠ **The Appearance pane no longer has a font row** (font choice lives in the new pane). The pane is **one table**: five built-in rows first (System / 粉圓 / 芫荽 / 源樣明體 / 源樣烏體), then the user's own — **the selected row = the font in use**; the candidate window switches on its **next** appearance (**no IME restart**). Table styling matches the custom-dictionary page: inset, no zebra striping, seven rows high, `+` / `−` as small bordered buttons. **`+`** picks a `.ttf` / `.otf` / `.ttc` (one you downloaded, not a system / built-in one) → appended at the end, **auto-selected**, candidate window switches. **`−`**: **disabled** while one of the five built-in rows is selected (built-ins cannot be deleted); enabled only on a custom row; **right-click** on a custom row offers Delete, on a built-in row it does **not**. Deleting the custom font in use → selection falls back to System, the candidate window uses the system font next time, the file is gone; deleting an unused one → the open candidate window is **not** affected. ⚠ **No pop-up windows of any kind** (USER 2026-09-08) — except the system's own file panel. **Reset to defaults**: Manage Typefaces has **no** such button (USER 2026-09-08; to go back to default pick the first row, System); the Appearance pane's button **no longer touches the font** (pick a non-system font, press Reset in Appearance, the font must stay as chosen). **Rejected files** (each must show an error alert and leave **nothing** in `Fonts/`): a `.txt` renamed `.ttf`, a real `.txt`, a folder renamed `.otf`, the original files of the four bundled typefaces (says "another typeface is already called …"). **Negative controls (unchanged)**: the five built-in options share **identical** row heights (only custom fonts measure their line box); the remaining Appearance rows (mode / layout / display) are still pop-up menus, and the size is one pop-up (S75). **File gone missing**: pick a custom font → move that file out of `Fonts/` in Finder → the candidate window falls back to the system font (does not go blank), the pane shows "The selected typeface is missing — using the system font" at the top, and `defaults read <bundle id> customFontFile` **still** holds that filename (a read must never silently rewrite the user's choice); move the file back → reopening the pane makes it effective again. Closing and reopening the settings window returns to the last pane viewed (including Manage Typefaces). ⚠ **Regression controls on the custom-dictionary page** (shared component extracted): `+` / `−` and pager button sizes, positions, disabled states, table height (10 rows), empty-table tray icon, "No results" when a filter matches nothing — all identical to main. macOS-only, engine untouched, no `make build` / `make dict`. ⚠ The Windows counterpart (P2–P4) is not yet done; that is S31.
  Status: pending

- **S31 Windows font-management pane + custom font upload** (2026-09-08, Windows counterpart of S30): Windows. The settings window sidebar gains a sixth row **Manage Typefaces** (**last**, below Manage Dictionaries, icon = Segoe Font glyph); the row label fits in all five display languages. ⚠ **The Appearance pane no longer has a font row**. The pane is **one `ListView`**: five built-in rows first (System / 粉圓 / 芫荽 / 源樣明體 / 源樣烏體), then the user's own — **the selected row = the font in use**. **`+`** picks a `.ttf` / `.otf` / `.ttc` (one you downloaded) → copied to `%APPDATA%\TaigiKeyboard\Fonts\`, added to the table, auto-selected. **`−`** is **disabled** on the five built-in rows; enabled on a custom row; pressing it deletes the file and the selection falls back to System. **Rejected files** (must show an error alert and leave **nothing** in `Fonts\`): a `.txt` renamed `.ttf`, a real `.txt`, a folder renamed `.otf`. ⚠⚠ **The biggest risk this round = hot reload**: after adding a font in Settings, an **already-open host** (Notepad + one WinUI app, not restarted) must draw the next candidate window with the new font; replace the same file in Explorer (same name, new content) → the next candidate window **must not** draw the old glyphs (mtime + length fingerprint changes the id and clears the `formats` / `ellipsis` caches); delete the file → the candidate window falls back to the system font, does not go blank. **Cross-process delete**: pressing `−` while a host is using that font may be refused by Windows — show an error, keep the row, allow a retry (do not pretend it was deleted). **Negative controls (unchanged)**: the five built-in options unchanged (identical row heights, only custom fonts measure their line box); the remaining Appearance rows unchanged; the custom-dictionary pane's `+` / `−` / pager untouched; in `settings.json` `fontType` is the string `custom` when a custom font is chosen and `customFontFile` is the filename; **an older build reading `custom` must fall back to the system font** (not crash). **AppContainer hosts** (no `%APPDATA%`) keep using defaults, no crash. Windows-only, engine untouched; **requires reinstall or dev install** (both the DLL and the settings exe changed).
  Status: pending

- **S32 Telex tone keys** (2026-09-09, roadmap § Desktop Telex, PR #17–#19): macOS + Windows both. General settings gains a **Tone Keys** picker (below Input Method), default **Standard**; choosing **Telex** shows a one-line legend under the picker, TL `z = ts(zh = tsh)`, POJ `z = ch(zh = chh)`. **Standard** (unchanged): `tai5` tone digit, candidate-window hints `q w d f z x v y ;`, `w` picks the 2nd, `;` picks the 9th, `⇧2` types `@`. **Telex / TL**: `tev` → té, `khooy` → khòo, `langd` → lâng, `kangw` → kāng, `titv` → ti̍t (was `titx` until S57), `taix` → tai1, `tsangq` → tsa̋ng; `zhi` → tshi, `zo` → tso; `taidfgiv` → tâi-gí; `tev` then `y` → tè (tone replaced); `tev` then `v` → still té (no change); Backspace deletes the tone; candidate hints become `1`…`9`, `3` picks the 3rd, `q w d f` **no longer** pick (they become tones / `f` hyphen); typing `Z` while composing → `Ts`; **with no composition**, `v` / `f` type `v` / `f` directly (no composition), `z` starts composing with `ts`. **Telex / POJ**: `zit` → chit, `zhiunnw` → chhiūⁿ, `chit` stays chit (`c` does not expand). **Shortcuts**: the slot-key row **is gone**; recording `v`, `;`, `3`, `⇧3` is refused (says it is a typing key), `⌃V` is allowed; a global shortcut previously recorded on bare `z` / `q` shows an empty row after restart. Engine changed → **`make build` first** (macOS `make -C macos install`; Windows reinstall / dev install).
  Status: pending
- **S33 Candidate-window toggle** (2026-09-09, roadmap § Desktop Telex, PR #20): macOS + Windows both. General settings gains a **Candidate Window** toggle (above Show Typed Text), default **on**. Off → typing `tai5` shows **no** candidate window, the composing underline stays; Space writes `tâi ` (with auto-space), Return writes `tâi` (**no** newline); Tab / `[` / `]` also commit; in Standard mode digits are still tones, in Telex mode typing `3` while composing commits then inserts `3` (with auto-space on this may be `tâi 3`, known). Turning it off in Settings while a candidate window is open → macOS drops the window at once, the composing text stays; Windows drops it on the next key; on both, Return right after writes the romanization, **not** a candidate. Turning it back on → the window returns on the next key. No `make build` (platform-only).
  Status: pending
- **S34 Telex guide shortcut** (2026-09-09, roadmap § Follow-up Telex guide, PR #21 macOS / #22 Windows): both platforms. **Shortcuts** gains a last row **Telex Guide**, default macOS `⌃⌘/`, Windows `Ctrl+Alt+/` (re-recordable). **The legend under Tone Keys in General settings is gone**. Pressing the shortcut → a card in the centre of the screen: title just **Telex** (same in all five display languages), two columns × nine rows (`x v` = tones `1 / 4` and `2 / 8` (S57), `y d w q` = tones 3 5 7 9; `z` / `zh` = initials, TL shows `Initial ts` / `Initial tsh`, POJ shows `Initial ch` / `Initial chh`; `f` = hyphen). **No example column, no "press Esc to close" line** (USER 2026-09-09). Press again → closes. **Holding the shortcut → toggles once only** (check especially on Windows). Card open + type `a` → card closes, `a` is typed. Composing `tai5` with the card open + Esc → card closes, `tai5` and the candidate window **both stay** (Esc does not cancel composition). Card open + input-mode switch shortcut → card closes, mode switches. Card open + open Settings (shortcut / menu bar) → card closes. Card open + click another window / switch app → card closes. Windows extra: in a read-only field (e.g. a locked text box) the shortcut opens the card, any key closes it; the card never takes focus, mouse passes through. No `make build` (platform-only); macOS `make -C macos install`, Windows reinstall / dev install.
  Status: pending

- **S35 The literal roman cell has no slot key** (2026-09-09, follow-up to §34/S22): macOS + Windows both. With **Show Typed Text on** (default), type `tai5` → the **first cell** `tâi` (literal) **has no slot key** (no `q` in Standard, no `1` in Telex); the **second cell** (first dictionary candidate, e.g. `台`) is `q` / `1`, the third `w` / `2`, and so on. **Pressing `q` → commits the second cell's word** (not the literal); the literal can still be committed with Return, with a **mouse click**, or reached with → / Space. **Only eight keys on the first page**: the ninth key (`;` / `9`) does **nothing** on this page (no reaction, does not commit the ninth cell). **Later pages**: → / PageDown to page two → its first cell has `q` / `1` again, all nine keys. **Vertical window**: after scrolling (selecting the 20th) the top row has `q` / `1` again. **Expanded grid**: after expanding, the selected row's first cell has `q` as usual when it is not the literal. **Toggle off** → the first cell is a dictionary candidate again with `q` / `1`, all nine keys (as on main). Check all three candidate layouts (horizontal / vertical / expanded). **The literal cell's text is vertically centred** (added 2026-09-09): in **Hanji–Romanization Pairing** mode (horizontal or expanded layout) type `tai5` → the first cell `tâi` has one line of text that is **vertically centred**, not stuck to the top with a blank line below; the second cell `台語` still shows Hanji above, romanization below. **Cell height unchanged** (the whole row is equally tall, baselines aligned). The vertical layout is inline (romanization after the Hanji), already centred, unchanged. **Romanization Only** / **Hanji with Romanization** modes are one line throughout, unchanged. **The literal cell's text is centred both ways** (added 2026-09-09): in **Hanji–Romanization Pairing**, horizontal or expanded layout, type `tai5` → press → to move the highlight to the second cell (**not** `q`, that commits), look at the first cell `tâi`: its text is **centred vertically and horizontally** (it has no slot key, so it does **not** skip past the key column like the others); from the second cell on the text sits after the slot key, the row aligned. The vertical (inline) layout keeps every row's text at the same x, no shift. ⚠ **The first cell has no background of its own** (a tinted background was tried 2026-09-09, USER judged it looked bad, removed) — the first cell differs by "no slot key", not by colour. **Negative controls (unchanged)**: Space picks the other script, Return commits the highlighted cell, ⇧Return commits the raw text, mouse click, paging keys, candidate window size / font all unchanged. No `make build` (platform-only); macOS `make -C macos install`, Windows reinstall / dev install.
  Status: pending
- **S36 Symbol picker** (2026-09-09, roadmap § Desktop symbol picker, P1 macOS #26 / P2 Windows): both platforms. **Shortcuts** gains a **Symbol Menu** row (below Switch Candidate Display, above Telex Guide), default macOS `⌃⌘,`, Windows `Ctrl+Alt+,` (re-recordable; `` ` `` is still Hanji/romanization swap, **untouched**). Press it **with no composition** → a window styled like the candidate window appears beside the caret, **one level, one flat table** (USER 2026-09-09 dogfood: two levels "would break the user's flow", flattened): punctuation first `，。！？；：、…`, then brackets **one pair per cell** `「」`, `『』`, `（）`…, then special symbols `←↑→…€£¥…★☆…`; slot keys follow the scheme (Standard `q w d…`, Telex `1 2 3`), using the user's candidate layout (horizontal / vertical / expanded). **The first key already picks**: press `q` → `，` is written, the window closes; page to the brackets and pick → both halves `「」` are written, **the caret rests after `」`** (same on both platforms; do not expect the caret in the middle). Return / Space write the highlighted cell; ←→ / ↑↓ / PageUp/Down / Tab / `[` `]` move and page (with the user's recorded paging keys); **Esc closes directly**, Esc is not passed to the host. **Negative control**: the window has **no** Punctuation / Brackets / Special Symbols category cells. Press the shortcut again → closes; **holding it toggles once only**. Window open + type `t` → window closes, `t` starts composing. **While composing** `tai5` with the candidate window open, press the shortcut → the highlighted candidate is committed first (as Return), then the symbol picker opens; composing without a candidate window (toggle off) → the romanization is committed, then it opens. **Auto-space on**: `taigi` → shortcut → the document reads `taigi ` (with space), pick `，` → `taigi， ` (punctuation and space swapped, same as typing `,` directly); moving a few cells with the arrows before picking must swap too. **Hanji-first**: page to the brackets and pick `()` → writes `()` (**not** `（）`; what you pick is what you get). **Candidate-window toggle off** → the symbol picker still opens. Window open + input-mode switch shortcut / open Settings / click another window / switch app → window closes. **Negative controls (unchanged)**: `` ` `` Hanji/romanization swap as before; candidate-window Space / Return / slot keys / paging keys unchanged; typing `,` directly under Hanji-first still gives `，`. Engine untouched, no `make build`; after macOS `make -C macos install` confirm `Contents/Resources/desktop-symbols.json` was copied in (otherwise the shortcut does nothing); Windows reinstall / dev install (the table is compiled into the DLL). **Windows extras**: hosts that draw candidates themselves (UI-less, e.g. some UWP apps) still get our own floating window for the picker; in a read-only field the shortcut is handed to the host (no picker); in English mode (Shift toggle) the picker still opens and the picked symbol is still written.
  Status: pending
- **S37 Composing caret moves left / right** (2026-09-09, roadmap § Desktop composing caret, P2 macOS / P3 Windows): both platforms. Composing `ka2` (shown `ká`) → macOS `⌥←` / Windows `Ctrl+←` **twice** (the first crosses the tone digit `2`, the on-screen caret does not move; the second lands between `k` and `á`, the host's caret line must visibly move), the candidate window **unchanged** (same candidates, same highlight, same page); type `h` → shows `khá`, candidates refresh to those of `kha2` (`跤`…) (caret at `kh|á`); `⌥→` **twice** back to the end (a third does nothing), type `i` → raw `kha2i` (shown literally as `kha2i`, a tone digit in the middle does not convert). `⌥←` at the front (`|ka2`) again → no reaction, no beep, composition stays. Backspace at the front → **no reaction** (no deletion, no cancel); Backspace with the caret in the middle → deletes the character before the caret. Telex mode (Tone Keys = Telex): `ka` → `⌥←` → type `v` → the tone lands on the chunk before `k` (no character before `k` → nothing), back to the end and `v` → `ká`. **Display mapping**: `ng5` (`n̂g`) → `⌥←` once → crosses the tone digit `5`, the on-screen caret **does not move**; second → `n̂|g`; type `k` → raw `nkg5` (shown literally). POJ `hoo` (`ho͘`) → `⌥←` once → the caret **appears not to move** (it crossed the second `o`, same display position), once more → `h|o͘`; `tinn` (`tiⁿ`) likewise, the first press is invisible. **After nailing**: `taigi`, pick `台`, nailed (Hanji-first shows `台gi`, romanization-first `台 gi`) → `⌥←` twice stops at `台|gi`, a third does nothing (never enters `台`); **under Hanji-first typing `h` → `台hgi`, no extra space** (the four-op config drift fixed this round; the old build gave `台 hgi`). **Negative controls (unchanged)**: candidate-window ←→↑↓ / Tab / `[` `]` / PageUp/Down / slot keys as before (horizontal ←→ per cell, ↑↓ page); ⇧← / → still commit then let the host select; Home / End / fn⌫ (Windows Delete) still commit and hand over to the host; **with no composition** `⌥←` / `Ctrl+←` is still the host's word jump; Esc cancels the whole span; after picking a candidate / Return / Space the host caret sits at the end of the committed text (also when committing with the caret in the middle). **Shortcuts**: after the composing rows, above Reset to Defaults, one more row **Move Composing Caret** with `⌥←  ⌥→` on the right (Windows `Ctrl+←  Ctrl+→`), **no recorder, not editable**; pressing Reset to Defaults leaves the row as is. All five display languages. Engine changed → **`make build` first** (macOS `make -C macos install`; Windows reinstall / dev install). iOS / Android need no test (the caret is always at the end).
  Status: pending
- **S38 ⇧ + slot key = pick a cell in the other script for this commit** (2026-09-10, roadmap § Desktop ⇧ + slot key): both platforms. TL side-by-side, romanization-first: type `taigi` → candidate window opens (slot 0 literal `taigi` no key, `w` = 台語 `tâi-gí`, `d` = 臺語 …) → press **`⇧W`** → the document receives **台語** (Hanji, same as `→` once then Space), **the highlight does not move**, window closes, auto-space follows the Space rule (no space after Hanji); `⇧D` → 臺語. **Hanji-first**: `⇧W` → `tâi-gí` (with auto-space). **Literal cell**: the literal `taigi` at slot 0 has no key → no ⇧ key reaches it; `⇧Q` picks the other script of the first keyed cell (the `q` cell). **Roman-only mode** (Candidate Display = Romanization Only): `⇧W` → swallowed, nothing written, window stays (like Space `.ignored`); **Hanji with Romanization**: `⇧` + the `台語` cell's key → `tâi-gí`, `⇧` + the `tâi-gí` cell's key → `台語`. **Telex** (Tone Keys = Telex): `⇧2` → the second cell's other script (on a US keyboard `⇧2` types `@`; recognised by key position); `⇧;` in Telex is `:` (commits the composition and writes `:`). **Empty slots**: on the last page a `⇧` key with no candidate → swallowed, no reaction. **Shortcuts**: after the commit-composition group, one more row **Pick a Cell in the Other Script** with `⇧Q … ⇧;` on the right (`⇧1 … ⇧9` under Telex; Windows `Shift+Q … Shift+;`), **no recorder, not editable**; change Tone Keys in General and come back → the right side follows; pressing Reset to Defaults leaves the row as is. Recording another row and pressing `⇧;` → refused (the "typing key" message, same as `⇧3`); `⇧[` can still be recorded. **Negative controls (unchanged)**: bare slot keys `q w d…` / `1…9` still pick the cell's own script; Space / Return / `⇧Return` / Tab / `⇧Tab` / `[` `]` / ←→↑↓ as before; `⇧←` still commits then lets the host select; **candidate window not open** (a single `t` with no candidates yet, or the toggle off) + `⇧Q` → `Q` (upper case) enters the composition, `⇧;` → commits and writes `:`; **no composition** + `⇧Q` → the host's upper-case Q; `⌃⇧Q` / `⌥⇧Q` → the host's; symbol picker open + `⇧Q` → as before (symbols have no other script); Windows: a lone short Shift tap still toggles Chinese/English, `Shift+W` does not. Engine untouched, no `make build`; macOS `make -C macos install`; Windows reinstall / dev install (DLL + settings exe both changed).
  Status: pending

- **S39 Custom font delete-then-re-add + row name = filename** (2026-09-10, PR #37): both platforms, no IME restart, no settings-window restart. **① Delete then re-add**: Manage Typefaces `+` add a custom font → select it, candidate window switches → `−` delete → **add the same file again** — it **must be accepted** (the old build said "another typeface is already called …" here); select it, the candidate window must draw with **the newly added copy**. Again: after deleting, add **a different file with the same name** (same PostScript name, different content) — the rendering must be **the new one**, not the old. **② Row name = filename**: add a font whose filename is **Chinese** (`源樣明體.otf`) → the row shows **`源樣明體`** (the old build showed `typeface` or the font's self-declared name); add `SnailFont-Pomacea.ttf` → shows **`SnailFont-Pomacea`** (case kept); add `jf-openhuninn-2.1.ttf` → shows **`jf-openhuninn-2.1`** (the inner dot kept, only the final extension dropped). Add **the same name twice** → the second row is `<name>-2`. **Negative controls (unchanged)**: the five built-in rows still carry translated strings (System / 粉圓 / 芫荽 / 源樣明體 / 源樣烏體) **unrelated to the font files**, and still change across the five display languages; built-in glyphs, row heights, candidate layout unchanged. ⚠ **Existing custom fonts on old installs are not revived** — their filenames were already flattened to lower-case ASCII, so after upgrade the row shows something like `iansui-regular`; the original Chinese name cannot be recovered; delete and re-add to fix. macOS + Windows local changes, engine untouched, no `make build` / `make dict`; Windows reinstall or dev install.
  Status: pending

- **S40 Adding a font no longer crashes; list selection stays put** (2026-09-11, fix for the Windows settings-window `0xc0000409` abort): **Windows only**, reinstall or dev install (settings exe changed). **① Add a font**: Manage Typefaces → `+` → pick an `.otf` / `.ttf` → the settings window **must not crash**, the new row is appended and **auto-selected** (highlight on it), the candidate window draws with the new font next time. Adding **three** fonts in a row must not crash. **② Delete**: select a custom row, `−` → row gone, selection falls back to the first row System, no crash. **③ Re-enter the pane**: after adding, switch to General → back to Manage Typefaces → **no crash**, and the highlight is still on the row in use (the first fix missed this; must test). **④ Custom dictionary** (same fix): Manage Dictionaries → Custom Dictionary → select a row → `+` add a new entry (new entries sort to the top, the selected row shifts down) → **no crash**, and the originally selected row **stays selected** (✎ / − still work and act on that row); import a CSV (several entries) → no crash, selection does not jump; type in the filter → the list shrinks → pick a row → no crash; page → pick a row → ✎ opens **that row's** content. **⑤ Selecting rows**: in Manage Typefaces click every row with the **mouse** and walk with the **keyboard** arrows → what you pick is what is used, never "picked but nothing happened" (the fix has one render frame with no selection; a fast press must not drop it). **Negative controls (unchanged)**: the five built-in fonts' names, row heights, candidate layout unchanged; `settings.json` `fontType` / `customFontFile` format unchanged; custom-dictionary `+` / `−` / ✎ / pager / empty-table text unchanged. Engine untouched, no `make build` / `make dict`. macOS has **no** such item (its list is not reactor-drawn).
  Status: pending

- **S41 Typing with a custom font no longer kills the host** (2026-09-11, DWrite factory lifetime fix): **Windows only**, reinstall or dev install (DLL changed). **① Type**: pick a **custom** font in Manage Typefaces → in **Notepad** type `taigi` (candidate window appears) → Notepad **must not crash**; repeat in a **browser** input, a **WinUI app**, and **Office**, none may crash. Picking candidates, paging, typing longer input (every cell repaints) must also hold. **② The font really changed**: the candidate window must render in the chosen font, not the system font — "no crash but fell back to the system font" is **a different failure**, report it. **③ Built-in negative controls (unchanged)**: switch back to System / 粉圓 / 芫荽 / 源樣明體 / 源樣烏體, all five type normally and look visibly different. **④ Replace the file**: delete the custom font in use in Settings → the next candidate window falls back to the system font, no crash; add a same-named file with different content → the candidate window draws **the new one**. **⑤ Already-open hosts**: an app not restarted while fonts were added / replaced must draw its next candidate window with the new font, without crashing. ⚠ The old symptom was `0xc0000005` in `DWrite.dll`, the host vanishes outright (visible in Event Viewer), not an IME-side crash — so "the app suddenly closed" is this item. Engine untouched, no `make build` / `make dict`. macOS has **no** such item.
  Status: pending
- **S42 Font-management lists installed typefaces (macOS)** (2026-09-11, PR1 of § Desktop installed typefaces): open Manage Typefaces → after the five built-in rows and the imported rows, **every** font family installed on this Mac is listed (⛔ no Hanji-coverage filter; Latin faces like Arial / Helvetica must be there too); families starting with `.` (hidden) do not appear; **built-in / imported fonts are not repeated** in the system section (芫荽, 源樣明體 only as their built-in row). **10 rows per page**, `n / N` + ‹ › pager bottom-right (as in the custom dictionary), the table itself does not scroll; the last page has exactly the remainder. **Search field** filters all three sections live (`源` finds 源樣明體 / 源樣烏體; `hel` finds Helvetica) and returns to page 1; clearing restores the full table; when the selected row is not on the current page `−` is disabled and the preference unchanged. `+` import jumps to the new row's page and selects it. Pick a system family → the candidate window switches **without restarting the IME**, Hanji + romanization both in that family (Latin faces fall back for Hanji). **Install a new font in Font Book** → with the pane open the list gains a row by itself; **remove the one in use** → the candidate window falls back to the system font, the pane shows the "The selected typeface is missing — using the system font" notice, no crash; reinstall → recovers without touching the setting. **Pane open time**: no perceptible difference from 3.6.8. **Import a font file already installed on the system** (even renamed; matched by the name inside the file) → no more `already called …`; instead the "This computer already has this typeface" notice, jump to that system row and select it; import a bundled font file (e.g. `fonts/font/iansui.ttf`) → same notice, jump to its built-in row. **Negative control**: the five built-in rows' glyphs, row heights, layout unchanged. macOS-only, engine untouched, no `make build`; `make -C macos install` before dogfood.
  Status: PASS 2026-09-11 (per project memory)
- **S43 Font-management lists installed typefaces (Windows)** (2026-09-11, PR2 of § Desktop installed typefaces): **Windows only**, reinstall or dev install; close every typing host before dogfood (the old DLL stays mapped). Settings Manage Typefaces: after the five built-in rows and the imported rows, **every** font family installed on this Windows (en-us names; Arial / Segoe UI / 微軟正黑體 `Microsoft JhengHei` must all be there), **10 rows per page** + `n / N` ‹ › bottom-right (as in the custom dictionary), the list itself does not scroll. **Search box** filters all three sections live and returns to page 1 (`jheng` finds Microsoft JhengHei; `tai` finds names containing `â`); clearing restores the full table; when the selected row is not on the current page `−` is disabled and the preference unchanged. Pick a system family → **already-open hosts** (Notepad + one WinUI app) switch on the next candidate window, no restart. **Import a font file already installed on the system** (even renamed) → the "This computer already has this typeface" notice + jump to that system row and select it, **no copy** left in `%APPDATA%\TaigiKeyboard\Fonts`. **Settings → Fonts, remove the family in use** (or uninstall in Font Settings) → the next candidate window falls back to the system font, the pane shows "The selected typeface is missing — using the system font", no crash; reinstall → recovers without touching the setting. **Pane open time**: no perceptible difference from 3.6.8 (one `GetSystemFontCollection(check_for_updates)` call). **Negative control**: the five built-in rows + custom rows render unchanged; the custom-dictionary pager's look / behaviour unchanged (extracted into a shared component).
  Status: PASS 2026-09-11 (per project memory)
- **S44 NextWord prediction row follows Candidate Display + the literal cell inherits the dictionary identity** (2026-09-12, USER 「Bug 2 is real bug」; iOS / Android + engine): **① Hanji with Romanization**: Settings Candidate Display → Hanji with Romanization, POJ type `sio2-chia2` → tap the `小姐` cell → the prediction row shows **a Hanji cell and a roman cell side by side** (`母 bú 妹 muē 仔 á …`, no subtitle, one line high); same reading different Hanji keeps one roman cell, same Hanji different reading keeps one Hanji cell; tap a roman cell → the document receives romanization (**with** auto-space); tap a Hanji cell → Hanji (Annotate in Brackets ON → `Hanji (romanization)`); after either tap the next prediction round follows, and the frequency row rises in Tab3. **② Romanization Only**: Candidate Display → Romanization Only, type `sió-chiá` the same way → tap slot 0 `Sió-chiá` (the literal cell, which absorbed the dictionary row) → the document receives `Sió-chiá` (not 小姐), **the prediction row appears, all roman** (the old build showed nothing here); tap a prediction → romanization only. **③ Side-by-side**: the prediction row is still the original two-line title/subtitle cells; tapping the literal cell `Sió-chiá` still shows **no** prediction (the dictionary row has its own cell, the literal keeps its pure-roman identity, unchanged). **④ The literal cell under Hanji with Romanization**: `sió-chiá` → tap slot 0 `Sió-chiá` roman cell (it absorbed 小姐's roman cell) → document `Sió-chiá`, **the prediction row appears** (identity = 小姐). **⑤ Hyphen side effect (deliberate)**: roman-only mode, TL: tap `tâi` first (literal cell, identity 台), then type `gi2` and tap the `gí` literal cell → composing area / document become `tâi-gí` (same as tapping a dictionary cell); the same steps in side-by-side mode keep `tâi gí`. **⑥ TPS**: prediction row unchanged (Hanji). Engine changed → `make build` (**not** `make dict`).
  Status: pending
- **S45 First key after switching is no longer lost (Android)** (2026-09-12, USER report: "after switching apps, the first key presses do nothing / the candidate bar doesn't appear"; Android only, platform-only change, no `make build`): **① Switch IME**: in a LINE / Messages conversation type a few characters with Gboard → switch to TaigiKeyboard → type `t` **immediately** (the moment the keyboard appears) → `t` appears in the document, the candidate strip has entries; then `ai` → `tai` continues normally. Repeat 10×, including a cold start (swipe the app away, return from recents). **② Switch app**: mid-composition in app A → switch to app B (with an input field that restores drafts, e.g. LINE drafts) → the first key works, the candidate strip appears; switch back to A → if the composing region is still there, typing continues; if the app cleared it, a fresh start adds no extra characters. **③ True tap-away control (invariant §13)**: composing `tai` → tap elsewhere in the document → composition ends (whether the strip clears instantly is the same as 3.6.8, not this round's scope); type `k` → starts `k` at the new position, **the old `tai` is not re-inserted, no `taik`**. **④ Tap at the end of the composition**: type `tai` → tap right after `tai` → type `k` → **`taik` keeps composing** (span re-attached; only a host without `setComposingRegion` support gives `taitaik` — record the app name). **⑤ Restart scenarios**: Chrome address bar, a Compose text field (this app's custom-dictionary add field), a WebView form — the first key works in each; an app `setText` mid-typing (paste / autofill) → no crash, no extra characters. **⑥ Password field**: the first key after switching to a password field works (non-composing path). **⑦ Negative controls**: ordinary typing, backspace, Enter / Space commit, candidate taps, NextWord predictions — no difference from 3.6.8; in ordinary hosts (LINE / Messages / Chrome) the first key after a switch has no perceptible delay (reconcile only calls `getExtractedText` once, synchronously, when the host reports no composing region while we are composing).
- **S46 Partial-tone input keeps the typed tone (all four platforms)** (2026-09-14, USER: "when I type teng5-sek, why is the word teng-sek ranked first? têng-sek ends up third instead"; §17 case 3, engine-only → `make build`, **not** `make dict`): fresh session, default source toggles. **① POJ** `teng5-sek` → the only two-syllable phrase in the strip is 程式 `têng-sek` (the roman literal, 程 and the other single-syllable tone-5 `têng` cells stay), no 等式 / 中式; `teng5sek4` and `tengsek` unchanged from 3.6.8 (only 程式 / every tone). **② TL** `ting5sik` → 程式 only; `ting5s` (before the second syllable closes) → only tone-5 extensions (程式 / 程序 / 程序…), no 中式. **③ Second syllable free** `teng5se` (POJ) → 程世 `têng sè` appears, 中西 `teng-se` does not. **④ Custom entry**: add a custom word with a tone-2 first syllable under the same reading (e.g. `tíng-sik` → 甲) → `ting5sik` does NOT show it, `tingsik` does; a custom `tîng-sik` → 乙 shows at slot 0 for `ting5sik`. **⑤ Negative controls**: toneless `taigi` / `tsiah`, fully-toned `tai5gi2`, English mode `tai5`, and every TPS item (S4 TPS / §41 space pin) — no difference from 3.6.8. Known gap, unchanged by this fix (record what you see): `tengsek4` (only the LAST syllable toned) still shows no 程式 — only the walker's whole-buffer synth, if any (頂色 on the default toggles).
  Status: pending
- **S50 Abbreviation input reaches the abbreviated words (all four platforms)** (2026-09-18, USER: "input 'ss', should return '鎖匙só-sî'" + "words with no initial consonant…", §46; engine + dictionary build, needs `make dict` **and** `make build`): TL, type `ss` → the strip shows the literal `ss` at slot 0 and then 2-syllable words whose syllables start with s + s — 先生 first, 鎖匙 `só-sî` within the first ten; tap 鎖匙 → the document receives 鎖匙 (Hanji mode) / `só-sî` (romanization mode), the composing buffer is fully consumed, and 鎖匙 rises in Tab3 frequency like any selection. `tk` → 逐家 / 大家 (**not** 出去 any more — `tshut-khì` is `tshkh`); **aspirates whole (USER first report)**: `tshkh` → 出去, `tsp` → 食飯, `khl` → 快樂 `khuài-lo̍k`; 披頭巾 is not in the dictionary — add it as a custom word `phi-thâu-kin` and type `phthk` (a custom word saved before the update must also answer once the app has relaunched: the side table is re-derived on the schema bump); `tt` → 親像 … (the largest abbreviation key, must not lag perceptibly); a long nasal chain such as `mngmngmngmngmngmngmngmngmngmngmngmngmngmngmngmngs` must not stall the keyboard. **Order rule**: `ts` → 這 / 做 / 早 … exactly as before, then the words whose leading units are ts + s (`tss` → 正實 / 但是 … reachable directly); `mk` → 毋 … as before, then the `m̄-k…` extensions, then 物件 / 妙計 …. POJ layout: `chp` → 食飯 `chia̍h-pn̄g`, `chhkh` → 出去; `ss` → 鎖匙 `só-sî`. TPS layout: `ㄙㄒ` → 鎖匙 (no space, no tone mark). **Negative controls (unchanged)**: `gi` → 語 / 義 / 議 … first, the `g…-i…` words (外移 …) only in the tail; `ginalangtsiahpngbesai` → 囡仔人食飯袂使 at slot 1, never 外夷 / 外移 anywhere; `s` / `s-s` / `s s` / `ss2` show no 先生 / 鎖匙; `si` / `tai` / `tai5` / `taigi` / `kesi` / `tsuisi` strips identical to before (S25 / §18); `ng` / `m` still their own syllables; TPS `ㄇ` → 毋, `ㄇㄒ` → 毋是 (§35, unchanged), `ㄒㄧ` + space (§41) unchanged; `ㄙㄒˊ` shows no 鎖匙. A custom word 鎖匙 `só-sî` typed as `ss` appears once (the custom row, ahead of the dictionary block), not twice. **Zero-initial abbreviations (USER second report)**: `aea` → 紅嬰仔 `âng-enn-á` and `iii` → 育幼院 `io̍k-iù-īnn` appear (after the syllabic candidates — scroll); `ai` → 愛 … first, 阿姨 in the tail; TPS `ㄤㆪㄚ` → 紅嬰仔. Tab3 Dictionary search `ss` still lists 鎖匙 / 先生 (acronym search kept through the family union). **Single-initial strips got better, not worse (side effect of the family split, §46)**: `t` → 佇 / 台 / 就 / 代 / 大 lead (previously only 乾 / 凋 / 焦); `s` → 是 / 時 / 事 / 死; `m` → 毋 … then 門 / 問; `a` → 後 / 俺 / 安 / 暗; `ph` → 破 / 皮 / 批; TPS `ㄇ` → 名 / 命 / 物, `ㄍ` → 講 / 工 / 機, `ㄉ` → 佇 / 台. Known and unchanged: 著 `tio̍h` / 頭 `thâu` do not show under bare `t` (shortest-first hydration budget), they do under `ti` / `th`.
  Status: pending
- **S51 Symbol picker keys stay in the picker under Chromium hosts (macOS)** (2026-09-18, Facebook video "with the symbol menu open, after pressing ↑↓←→ the typing position moves by itself, and pressing 'Enter' types nothing", §47; macOS only, `make -C macos install`, no `make build`): in **Chrome** (any web textarea, e.g. a Discord / Slack message box in the browser) and in an **Electron app** (Discord / Slack / VS Code), put the caret mid-line after some text, press `⌃⌘,` → the picker opens and **one underlined space** appears at the caret. Press `→` `→` `↓` `←` → only the picker highlight moves; the page's caret and the underlined space **do not move**. Press Return on a one-character symbol (`，`) → `，` replaces the underlined space, nothing else happens (**a chat box does not send**); repeat and pick a bracket pair `「」` → both halves land, caret after `」`. Esc → the underlined space vanishes, the text around it is untouched, no newline, nothing sent. Chord again with the picker open → same clean close. Picker open + type `t` → the space is gone and `t` composes at the same spot. **Selection edge**: select a word, press the chord → the picker opens with **no** underlined space (nothing is marked over a selection); press Esc → the selected word is still there; chord again, pick `，` → the word is replaced by `，` (as typing would). In Chrome this one case still leaks the arrows to the page — by design, the selection is worth more. **Auto-space**: `taigi` → Return → `taigi ` → chord → `q` → `taigi， ` (the swap survives the placeholder). Click elsewhere in the document with the picker open → the picker closes and the underlined space vanishes (IMK reports the click outside a marked region as `commitComposition`; if the space stays, report it). **Negative controls**: TextEdit / Notes behave exactly as S36 says; the composing candidate bar's arrows / Return unchanged in Chrome; `` ` `` Hanji/romanization swap unchanged. **Windows (no code change — verification only)**: TSF eats the key from the key sink with no composition (`session.rs::symbol_picker_takes_key`), the same shape PIME 新酷音's `` ` `` symbol menu relies on (`references/PIME/python/textService.py:266` `isComposing = compositionString != "" or showCandidates`), so no placeholder is expected there; run the same keys in **Chrome** and **Notepad** on the dev box (host started AFTER the install): picker up → `→` `→` `↓` `←` move only the highlight, Return writes `，` and nothing else, Esc closes with no newline. A leak there is a NEW Windows finding (report host + whether the composing candidate window's arrows leak too).
  Status: pending
- **S52 Hanji-first out of the box, and the desktop General pane owns its output script and reset (all four platforms)** (2026-09-18, USER: "confirm the default output is Hanji everywhere, not romanization" + "fix them together: by default Hanji is output first, i.e. Hanji is the title and romanization the subtitle" + "add a 'Reset to Defaults' button at the bottom", §48; no engine change, no `make build`; macOS `make -C macos install`, Windows installer from CI, mobile app builds): **Fresh install / never-toggled install on every platform**: type `taigi` → the side-by-side cell leads with 台語 and carries `tâi-gí` as its subtitle / annotation; Return (mobile: tap) writes **台語**; typed `,` → `，` (full-width follows the hanji lead). An install that HAD pressed the 文/A key (mobile) or `` ` `` (desktop) keeps whatever it stored — a user on romanization stays on romanization after the update. **macOS / Windows General pane** (order since 2026-09-18 "reorder the 'General' settings" + "no groups; output and input can sit together": Input Script · Output Script · Tone Input · Auto-Space · Show Candidate Window · Show Typed Text · Display Language ‖ Version ‖ Reset to Defaults — one run, no sub-groups; mobile's label stays Input Mode): the **Output Script** pop-up sits right under Input Script, reads **Hanji** on a fresh install; pick romanization → the open composition (type `taigi` again) commits `tâi-gí`, subtitle 台語; press `` ` `` → the pop-up flips to Hanji without the window being reopened, and the reverse; set Appearance › Candidate Display = Romanization Only → the Output pop-up greys out (as the `` ` `` chord is inert there), still showing what is stored; back to Hanji–Romanization Pairing → enabled again; under Hanji with Romanization the pop-up stays enabled and, like the chord, only decides punctuation width. **Reset to Defaults** at the foot of the pane (same row style and text as Appearance / Shortcuts / Manage Dictionaries): set Output = romanization, Input Method = POJ, Tone = Telex, Display Language = English, Auto-Space on, Show Candidate Window off, Show Typed Text off → press it → every row returns to its default (TL / Hanji / Standard / off / on / on / System Language) and the window re-reads in the shipped language; Appearance rows, Shortcuts chords, Dictionary toggles and the update row are untouched. **Mobile 文/A key**: on a fresh install the key is **plain** (iOS outlined `character.square`, Android normal key colour) and the strip leads with 台語; tap it → the key **lights** (iOS filled, Android `key_bgColorActive` grey), the strip leads with `tâi-gí`, `,` types `,`; tap again → plain, 台語, `，`. Theme tab preview shows the plain key with the 選 (select) confirm label. **Negative controls**: `` ` `` under Romanization Only display still does nothing; Hanji with Romanization Space-commits-the-other-script unchanged (there the key still flips only the width, lit = half-width); S33 / S22 rows unchanged; mobile settings › reset lands on Hanji too.
  Status: pending
- **S53 Symbol picker: `⋯` and the recent picks lead (macOS + Windows)** (2026-09-19, USER: "add '⋯' to the symbol menu" + "the symbol menu should sort by recent input"; no engine change, no `make build`; macOS `make -C macos install` — confirm `Contents/Resources/desktop-symbols.json` was copied; Windows reinstall / dev install, table compiled into the DLL): **Fresh picker** (no pick yet — `defaults delete com.siansiansu.inputmethod.TaigiKeyboard recentSymbols` / remove `recentSymbols` from `%APPDATA%\TaigiKeyboard\settings.json`): press the chord → the list is the file order, `，` first, and `⋯` sits **right after `…`** on the punctuation page (both ellipses on one page). **Recents**: pick `。` (`w`) → chord again → `。` is now the **first cell** (`q`), then `，` `！` … in file order with `。` gone from its old place; pick `「」` → next opening reads `「」 。 ， ！ …`; pick `。` again → `。 「」 ，…` (latest leads); pick nine different symbols, then a tenth → the first of the ten has **dropped back to its file place** (the recent strip is one page of nine, so page two onward is still the file order — brackets together, specials together). **Persistence**: quit and relaunch the input method (macOS: log out / in, or `make -C macos install`; Windows: close the host and open a new one) → the recents are still in front; a new controller (switch to another app and back) sees them too. **Auto-space**: `taigi` → Return → `taigi ` → chord → pick `，` → `taigi， ` (the swap still fires from a recent cell); pick `⋯` after `taigi ` → `taigi ⋯` (an ellipsis is not an attaching mark, the space stays). **Negative controls**: Esc, walking with the arrows, an empty slot on the last page → the next opening is unchanged (only a write records); General › Reset to Defaults leaves the recents alone; `` ` `` Hanji/romanization swap, the composing candidate window, S36 / S51 rows unchanged; `⋯` under Hanji-first → `⋯` (what you pick is what you get). **Windows extra**: a pick with the settings window open on General → the symbol lands and the window's rows keep their values (the write goes under the file lock like the Ctrl+Alt+H chord).
  Status: pending
- **S54 Custom theme: one background surface, gradient direction, scheme-invariant colours, new editor order (iOS PR A; Android PR B)** (2026-09-19, USER: "no need to separate the candidate-bar background from the keyboard background" + "add a background gradient, with a configurable gradient direction" + "custom themes don't need a light/dark mode split" + "rearrange the order of the controls"; no engine change, no `make build`; app build only): **Editor order** — Theme › New Theme… → the form reads Background (segmented Solid | Gradient, then one Keyboard Background row) · Keys (Normal Key Fill · Special Key Fill · Key Text · Key Corner Radius · Key Border Width · Key Shadow · Keyboard Height · Key Font Size) · Candidate Bar (Candidate Text · Candidate Text Size) · Reset to Defaults, preview pinned below; **no Candidate Background row anywhere**. **Seeded draft**: a fresh New Theme shows a light grey keyboard with white keys, grey special keys and black glyphs in the preview even while the phone is in dark mode; every colour row already has a concrete swatch and no reset arrow; pick any colour → the arrow appears → tap it → the seed colour returns. **One surface**: pick Keyboard Background = red → the preview's candidate bar AND keyboard body are both red, no seam between them; save → the real keyboard in Notes shows the same red bar + body. **Gradient**: tap Gradient → the preview shows a red→lighter-red top→bottom gradient (start = the solid colour you had), rows become Start Color · End Color only (**no Direction row** — follow-up E, 2026-09-19 USER: "drag with a finger to choose the gradient centre instead of using buttons", both platforms), and a white axis + arrowhead pointing down appears over the preview; press anywhere on the preview and drag toward the top-right → the arrow follows the finger, the gradient re-orients live, and near ↗ it snaps with a light tick; drag to an in-between direction → the gradient runs at that angle (no snapping outside ±6° of a preset); a finger on the preview no longer presses the preview keys; tap Solid → the arrow disappears and the preview keys press again; with VoiceOver / TalkBack on, the preview pointer is one element "Direction, 180°" and swipe up / down (TalkBack: volume keys or the adjust gesture) steps 45° at a time. Set Start Color = blue, End Color = yellow → the preview updates on each pick; tap Solid again → a single blue keyboard (first stop kept). Save → in Notes the gradient runs continuously from the candidate bar through the keys with the chosen direction; open the expanded candidate overlay (long candidate list ▼) and the symbol / layout / settings panels → each repaints the same gradient with **no seam** at the toolbar edge and no restart of the gradient at the panel top; rotate to landscape → still continuous. **Scheme-invariant**: with the custom theme applied, toggle Settings › Appearance dark / light (or the system toggle) → the keyboard's colours do not change at all (bar, body, keys, glyphs, candidate text). Apply Classic › Default → the keyboard follows light / dark again (negative control). **Old themes** (device that had a custom theme from ≤ mobile 3.6.8 with some rows untouched): after the update, the theme no longer flips with dark mode; the rows the user had set keep their colours; a theme that had Candidate Background set now shows the keyboard colour on the bar instead. **Built-in themes** (Sakura / Sea Breeze / Catppuccin, all three families): identical to before — vertical gradient, transparent bar, overlays continuous. **Card preview**: the custom shelf's card shows the gradient with its direction behind the sample key. **Liquid Glass** (iOS 26 device, Classic Default): unchanged pass-through; a custom theme still paints opaque. S6 / S33 rows unchanged.
  Status: pending
- **S57 Telex `x` = tone 1 / 4, `v` = tone 2 / 8 (macOS + Windows)** (2026-09-19, roadmap § Follow-up: Telex keys for tone 1 and tone 4; engine-only → **`make build` first**, macOS `make -C macos install`, Windows reinstall / dev install): Tone Keys = **Telex**, TL. **Open syllables**: `taix` → shown `tai1` in the preedit, candidates only the tone-1 `tai` family (呆 胎 …, no 台/tâi); `tev` → té (unchanged); `taix` then `v` → tái (replaced); `tai` + `d` then `x` → `tai1` (tone 5 replaced by 1). **Checked syllables**: `sitx` → `sit4` (實 is NOT offered — that is `si̍t`), `titv` → ti̍t (tone 8 now on `v`), `sitx` then `v` → si̍t, `sitv` then `x` → sit4, `sitx` then `x` → still sit4 (no change), Backspace removes the digit. `h` coda counts: `ahx` → ah4, `ahv` → a̍h. **Nasal / open codas are open**: `tinnx` → tinn1 (tiⁿ), `kangx` → kang1, `mx` → m1. **`y d w q` ignore the coda**: `sity` → sit3 (invalid, no candidates, same as before). **Continuous**: `taixgiv` → tai1-gi2 … the first syllable is pinned to tone 1 and the second starts on `g`; `tai5-` + `x` → nothing (tone on a hyphen). POJ: same keys, `sitx` → sit4, `chitv` → chi̍t, `c` still a plain letter (`chia` types). **Telex Guide card**: rows now read `x` Tone 1 / 4, `v` Tone 2 / 8, then `y d w q` 3 5 7 9, `z` / `zh` / `f` unchanged (check English `Tone 1 / 4`, Japanese, POJ / TL display languages). **Negative controls (unchanged)**: Standard scheme `tai1` / `sit4` / `sit8` digits as before; `x` and `v` with no composition still type `x` / `v`; Shortcuts recording `x` / `v` still refused (typing key); candidate slot keys `1`…`9` under Telex unchanged; iOS / Android untouched.
  Status: pending
- **S55 Custom theme: photo background (iOS PR C; Android PR D)** (2026-09-20, USER: "add uploading a photo as the keyboard background; mind the resizing and the aspect ratio, and the saturation must not be too loud"; no engine change, no `make build`; app build only): Theme › New Theme… › Background › tap **Photo** → the preview keeps its solid grey (nothing picked yet) and a **Choose Photo** row appears; tap it → the system photo picker opens without any permission prompt; pick a landscape photo → the row reads **Change Photo** with a thumbnail, a **Fade** slider appears at 35%, and the preview shows the photo filling the candidate bar + keyboard (centre-cropped, no letterboxing, no stretching), visibly softened (paler than the original, a light white veil because the key text is black); set Key Text = white → the veil turns dark; drag Fade to 0 → the photo shows only desaturated; to 80% → nearly flat; pick a tall portrait photo → still fills width and height, centred. Save → in Notes the keyboard shows the same framing as the preview; open the expanded candidate overlay and the symbol / layout / settings panels → each shows the photo continuing from the toolbar edge with **no seam or jump**, and the extension does not stall or get killed (memory); rotate to landscape → re-framed, still continuous. **Replace**: Change Photo → pick another → old framing replaced, no flash of the old photo. **Orphans**: pick a photo, then Back without saving; save another theme later → the App Group `theme_images/` holds only the photos referenced by saved themes (Files app › On My iPhone › TaigiKeyboard, or Xcode container download). **Delete** the photo theme → its file is gone on the next mutation. **Switch kinds**: Photo → Solid lands on the seed grey; Solid → Photo keeps the grey until a photo lands. **Card preview**: the custom shelf card shows the softened photo behind the sample key. **Negative controls**: Classic Default / gradient themes unchanged; S54 rows unchanged; Liquid Glass off under a photo theme.
  Status: pending
- **S58 Shortcut recorder takes Enter on any composing row, and bare Tab (macOS + Windows)** (2026-09-19, USER: "'Reverse Output', 'Output in the Other Mode': users report the Enter key cannot be used" + "check whether Shortcuts has similar bugs"; platform-only, no `make build`; macOS `make -C macos install`, Windows reinstall / dev install): Shortcuts pane. **Enter moves**: click Output in the Other Mode → press ↩ → the row reads **↩** (Windows `Enter`), **Confirm Key empties** (no beep, no message), Output What You Are Typing still ⇧↩; compose `tai5` → ↩ → the highlighted candidate lands **in the other script** (romanization under Hanji output, Hanji under romanization); Space now types a space after committing (the row is no longer on Space); ↩ with the candidate window off still commits `tâi` as typed. Click Confirm Key → press Space → confirm is Space; ↩ on Next Candidate → Output in the Other Mode empties, ↩ walks the list. **Refill**: with confirm on ↩ and literal on ⇧↩, clear Confirm Key with × → it refills to ↩ at once; put ↩ on Next Page then clear Confirm Key → it stays **empty** (both defaults held); clear Next Page with × → Confirm Key refills to ↩ in the same repaint (the key is free again). **Tab records**: click Next Page → press ⇥ → the row reads **⇥**, Next Candidate empties, focus does **not** jump to the next field; ⇧⇥ still records as ⇧⇥; Esc and a click outside a recording field still leave it unchanged; with no field recording, ⇥ still walks the form. **Negative controls**: bare `a` / `3` / `;` / ⇧3 still refused with "Already in use" + beep; ↑↓←→ still refused; bare Esc leaves the field unchanged, bare Backspace blanks it; global rows (Switch Input Mode …): bare ⇥ now records on macOS (same class as the bare `` ` `` they may hold — it takes ⇥ from the host while the input source is selected), Windows still refuses it "Already in use" (not a preserved-key name); Ctrl+Alt+A still records on Windows; Reset to Defaults → ⇥ ⇧⇥ ] [ ↩ ⇧↩ Space on the seven rows.
  Status: pending
- **S59 Input-source menu lists the global shortcuts (macOS + Windows)** (2026-09-19, USER: "the menu that appears on right-clicking the input-method icon in the desktop menu bar: I'm considering showing the global shortcuts there"; platform-only, no `make build`; macOS `make -C macos install`, Windows reinstall / dev install): click the menu-bar input-source icon (macOS) / the tray `台` button (Windows) → the menu reads **Switch Tâi-lô or Pe̍h-ōe-jī ⌃⌘C · Switch Candidate Display ⌃⌘H | Settings ⌃⌘S | Check for Updates** (Windows `Ctrl+Alt+C / H` and `Ctrl+Alt+S` in the accelerator column; Check for Updates has no chord). **Click**: with a text field focused, click Switch Tâi-lô or Pe̍h-ōe-jī → the mode HUD flashes and the next syllable composes in the other romanization; click Switch Candidate Display → the HUD names the next mode (Hanji–Romanization Pairing → Hanji with Romanization → romanization), an open candidate list is re-fetched in place. **Follows the recorder**: Shortcuts → re-record Switch Tâi-lô or Pe̍h-ōe-jī as ⌃⌥J → reopen the menu → the row reads ⌃⌥J; clear the row with × → the row prints no chord but still clicks; Reset to Defaults → ⌃⌘C again. **macOS bare key**: re-record a global row onto bare `z` → the menu row prints **no** chord (the pane still shows `z`), and typing `z` in TextEdit still types `z`. **Double fire**: press ⌃⌘C once → the romanization flips **once** (not twice). **Negative controls**: Switch Hanji/Romanization Mode (`` ` ``), Open Symbol Menu (⌃⌘,) and Open Telex Guide (⌃⌘/) are **not** in the menu (USER 2026-09-20: "very few people use it"); Settings still reopens the last pane; Check for Updates still opens General; a language change (Settings › Display Language › English) relabels the rows on the next open (`Switch Tâi-lô or Pe̍h-ōe-jī` …). Windows extra: open the menu with no text field focused → Switch Tâi-lô or Pe̍h-ōe-jī still flips (HUD at the last caret).
  Status: pending
- **S56 Closed tone-8 syllable no longer trails its shorter prefix family (all four platforms)** (2026-09-19, USER: "a user reports that after typing iah8, unrelated words such as ioh8 still appear among the candidates", §18 guard (d); engine-only → `make build`, **not** `make dict`): TL, type `iah8` → the strip is ONLY the `ia̍h` family (抑 役 也 葉 驛 …); no tier-2 `ia` tail (也/iā 耶 野 夜 椅仔 …). Same for `ioh8` (藥 鑰 葯 only — no 育 搖 腰 tail), `iok8`, `tiah8`, `tsiah8`. **Negative controls**: `iah` (no digit yet) → `ia̍h` family THEN the `ia` family (mid-typing pending tail kept); `iakau3` → 耶教 + the `ia` family (`kau3` continues from `ia`); `kah8` / `ah8` unchanged (only the full syllable); `tai5g` → 台 / 臺 still shown with `g` pending; S5 rows unchanged. TPS: `ㄧㄚㆷ˙` → only the `ㄧㄚㆷ˙` family, no `ㄧㄚ` tail; `ㄧㄚㆷ` (no mark) keeps the `ㄧㄚ` tail.
  Status: pending
- **S60 No Hyphens: candidates and commits drop the dictionary hyphen, khinsiann `--` becomes `·` (all four platforms)** (2026-09-20, USER: "evaluate adding a No Hyphens toggle… tâi-uân -> tâiuân… the khinsiann mark '--' is replaced by a dot '·'" + "only strip the ones the engine / dictionary produce" + "the user's custom dictionary must follow this setting too", §49; engine → `make build`; macOS `make -C macos install`, Windows reinstall / dev install): turn on **No Hyphens** (mobile Settings › Input under Annotate in Brackets; desktop General under Output Script). TL, romanization output, type `taiuan` → 台灣 reads **`tâiuân`** and commits `tâiuân`; `hoogua` → 予我 reads **`hōo·guá`**; `tshitniumasenn` → `tshitniûmásenn`; a custom entry `só-sî`/鎖匙 typed `sosi` → `sósî`. POJ: `taioan` → `tâioân`. Continuous: `tai` → 台, `uan` → 灣 commits **`tâiuân`** (compound glued, no space); side-by-side / Annotate in Brackets show the same hyphenless roman. Mobile after committing 台灣, the next-word strip shows hyphenless predictions (`lâng` … `tâiuânlâng`); tap one → re-type the pair → it still ranks first (association learned). **Negative controls**: type `tai-uan` → slot 0 literal reads **`tai-uan`** (typed hyphen kept) beside `tâiuân`/台灣; `--ah` → `--ah` / Hanji `--矣` unchanged (S8); TPS layout (`ㄊㄞˊㄨㄢˊ`) unchanged, Annotate in Brackets under TPS still `(ㄊㄞˊ ㄨㄢˊ)`; Hanji output commits 台灣 as before; toggle OFF → `tâi-uân` / `hōo--guá` exactly as today; re-type `taiuan` after committing 台灣 with the toggle ON → 台灣 still ranks first (frequency key untouched); desktop Reset to Defaults clears the switch.
  Status: pending
- **S61 About page from the input-source menu (macOS + Windows)** (2026-09-20, USER: "add github… discord… email below the 'General' settings" → "use FontAwesome" → "split that bottom section into a simple 'About the Keyboard' page… it doesn't need to appear in the settings menu… right-click in the menu bar… About… my logo isn't needed" → "remove the current version, put that description text back… apple style… use a page layout, icons are too small, and make Sponsor a button too"; platform-only, no `make build`; macOS `make -C macos install`, Windows reinstall / dev install): open the input-source menu → after Check for Updates in the same group a row reads **About TaigiKeyboard** (no chord); click it → the settings window opens on a page titled About TaigiKeyboard with **no sidebar row highlighted**, in the same grouped cards as the other panes: card 1 = two left-aligned lines, no heading (台語齒盤是自由、開源 ê 台語拍字家私，永遠免費、無廣告。 / 這个計畫目前由我一个人利用上班以外 ê 時間開發佮維護…), then one card of five link rows **Sponsor (♡) / taigikeyboard.tw / GitHub / Discord / info@taigikeyboard.tw**, each with its Font Awesome mark (16 pt, grey) at the left and ↗ at the right, the whole row clickable (Windows: the row lifts a subtle fill under the pointer); under them `© 2026 Bîn-hiân` as fine print. **No name, no version line, no icon.** **Click**: the Sponsor card → ECPay; the Discord card → `discord.gg/kXhtQfWvK`; the website card → `taigikeyboard.tw`; GitHub card → `github.com/taigikeyboard`; the email card → the mail client with `info@taigikeyboard.tw`. Dark mode: marks and text turn light, the Discord mark's eyes stay two holes (`F1` fill rule on Windows). **Negative controls**: General has **no footer** any more (the update row and Reset to Defaults end the pane; Current Version still shows on the update row); the sidebar lists the six panes as before, none named About; click General in the sidebar from the About page → the highlight returns; quit and reopen Settings after visiting About → the window reopens on About (last-used pane, as any other); Settings › Display Language › English → the menu row reads **About TaigiKeyboard**, the page reads in English (TL / POJ display languages still show the Hanji paragraphs until the USER supplies romanized copy).

  Status: pending
- **S62 Learned phrases — a phrase composed segment by segment becomes one candidate (all four platforms)** (2026-09-20, USER: "after the user types 'kikhilai' and then picks candidates one after another, e.g. '記' -> '起' -> '來', does the input method work out that typing 'kikhilai' means '記起來'?" → 「ok, plan it」; engine PR #109 + platform PR → `make build` first): Dictionary › 台日大辭典 OFF (the default); type `kikhilai`, pick 記 then 起來 (or 記 / 起 / 來) → the buffer commits; type `kikhilai` again → **記起來 is the first hanji candidate** (the §34 literal `kikhilai` still sits at slot 0); (no toggle: learning is always on, USER 2026-09-20). **Own store (USER 2026-09-21: "not recommended… it gets muddled" → "1. no UI needed 2. not carried")**: Custom Dictionary does NOT list `kì-khí-lâi → 記起來` — no row, no badge, the entry count unchanged; Export CSV / `.taigi` → no learned row in the file. Wipe: desktop Custom Dictionary pane › Clear Learning Records / mobile Settings › Reset to Defaults (user data) → the next `kikhilai` shows 機起來 again. Restart the keyboard / IME after learning → 記起來 still offered (the store persists and re-opens). **Negative controls**: 台日 ON → the dictionary 記起來 leads and the pair is listed once; add 既起來 `kì-khí-lâi` by hand → it leads, 記起來 still offered; delete that manual row → 記起來 still offered (stores are independent); typing `kikhi` (a prefix) never offers 記起來; a manual row keeps Enable Custom Dictionary as its own gate (turn it OFF → learned rows still offered); a dev device that reached custom-dictionary v5 / v9: after update the list holds only manual rows. Perf: S1–S3 unchanged; no keyboard dismiss after 50 learns.
  Status: pending
- **S63 Ctrl + punctuation types the other width once (macOS + Windows)** (2026-09-20, USER: "typing a full-width or half-width comma needs a switch with `; do you see a faster way?" → "adopt 1" → reversal question "after switching Hanji/romanization mode, does it flip?" → 「ok,go」; PR #119, platform-only, no `make build`; macOS `make -C macos install`, Windows reinstall): in Hanji-first (the default) type `⌃,` / `Ctrl+,` → **`,`** half-width lands, no candidate bar, mode unchanged (the next bare `,` is still `，`); press `` ` `` to romanization, type `⌃,` → **`，`** full-width lands, the next bare `,` is still `,`; `⌃⇧,` in romanization → `《`; `⌃[` in romanization → `「` (not an Escape); type `tai5` then `⌃⇧?` → in Hanji-first the commit reads `tâi?`, in romanization `tâi？`, one mutation. Auto-Space ON, romanization: type `gua2`, Space, `⌃,` → `guá， ` (the flipped comma takes the armed space); Hanji-first same steps → `guá, `. Shortcuts pane, Output block: a greyed row **Punctuation in the Other Width** shows `⌃, ⌃. ⌃;` (Windows `Ctrl+, Ctrl+. Ctrl+;`) after the Hanji/romanization row. **Negative controls**: `⌃⌘,` / `⌃⌥,` / `Ctrl+Alt+,` reach the host (nothing typed by the IME); `⌃-` and `⌃"` are the host's (hyphen and straight quote are not in the map); record `⌃,` on a Shortcuts row (e.g. Confirm Key) → that row wins over the flip until cleared; Windows: VS Code `Ctrl+,` opens no Settings while the IME is active (the chord is ours, as under 新注音), and Windows Terminal / an Electron host delivers the chord to the IME at all — report which hosts do not.
  Status: pending
- **S64 Shortcuts pane shows the three fixed rows: Pick a Candidate Directly / Move Through Candidates / Delete What You Are Typing (macOS + Windows)** (2026-09-20, USER: "for desktop, Shortcuts menu, think about what other operation hints we can show for users" → "Cancel What You Are Typing -> Delete What You Are Typing, the rest go"; platform-only, no `make build`; macOS `make -C macos install`, Windows reinstall): open Settings › Shortcuts. Candidate Selection block, after Next Candidate / Previous Candidate / Next Page / Previous Page: greyed **Pick a Candidate Directly** `qwdfzxvy;` (Tone Keys = Standard) — switch General › Tone Keys to Telex → the row reads `123456789` and the Hanji/romanization row below flips to `⇧123456789` / `Shift+123456789` in the same repaint; greyed **Move Through Candidates** `← → ↑ ↓ ⇞ ⇟` (Windows `← → ↑ ↓ PgUp PgDn`); then the existing Fix What You Are Typing row. Output block, after Confirm Key / Output What You Are Typing: greyed **Delete What You Are Typing** `⎋` (Windows `Esc`), then the Hanji/romanization and punctuation rows. Every greyed row: no field, no × button, a click records nothing. Display language TL / POJ / English / Japanese → the three labels switch with the rest of the pane. **Negative controls**: Reset to Defaults leaves the greyed rows as they are; type `tai5` and press Esc → the composition is gone and nothing lands (the row describes what already shipped, no behaviour change); recorder rows above and below still record.
  Status: pending
- **S65 Settings rows in pipeline order (macOS + Windows)** (2026-09-21, USER: "for Shortcuts, the General menu, Appearance and so on, think about the ordering; no grouping, just order them by logic" → the full version + all four ★; platform-only, no `make build`; macOS `make -C macos install`, Windows reinstall): **sidebar** top to bottom General · Appearance · Shortcuts · Manage Dictionaries · Custom Dictionary · Manage Typefaces (Manage Dictionaries before Custom Dictionary). **General**: Input Script · Tone Keys · Show Candidate Window · Show Typed Text First · Output Script · No Hyphens · Auto-Space · Display Language ‖ Version ‖ Reset to Defaults. **Appearance**: Appearance · Candidate Window Layout · Candidate Window Size · Candidate Display · Candidate Text Size ‖ Reset to Defaults. **Shortcuts › Output**: Confirm Key · Output What You Are Typing · Output the Other Script (Space) · Output the Other Script (⇧slot) · Punctuation in the Other Width · Delete What You Are Typing (Esc, last). **Shortcuts › Other**: Switch Tâi-lô or Pe̍h-ōe-jī · Switch Hanji or Romanization · Switch Candidate Display · Open Symbol Menu · Open Telex Guide · Open Settings Menu. **Negative controls**: every row still binds to the same setting (flip Output Script → `` ` `` state follows; record a chord on Open Settings Menu → the last row takes it, Reset to Defaults puts `⌃⌘S` / `Ctrl+Alt+S` back); the pane last opened is still the one that reopens (sidebar raw values unchanged); S64 fixed rows unchanged apart from the Esc row moving to the end.
  Status: pending

- **S66 Typed separator joins the continuous best reading (all four platforms)** (2026-09-21, USER: "consider letting users decide the separator, i.e. not always a space, but judging by the separator the user types" → 「ok,go」; PR #129, engine-only → `make build` first; macOS `make -C macos install`, Windows reinstall): TL, continuous input, type `goa-si` → the first hanji candidate 鵝仔是 reads **`gô-á-sī`**; `goa--si` → **`gô-á--sī`**; `goasi` → `gô-á sī` exactly as today; `tai-gi-si-kai` → `tâi-gí-sī-kái`; select the candidate under romanization output → the typed form commits. POJ: `goa--si` → `góa--sī`. No Hyphens ON: `goa--si` → `gôá·sī`, `goa-si` → `gôásī`, slot-0 literal still `goa-si`. **Negative controls**: `ki--khi-lai` → 記起來 still reads **`kì--khí-lâi`** (one dictionary word, its own form); `ki-khi-lai` → 機起來 `ki-khí-lâi` (S69: the plain `-` is not 記起來's `--`); `hoo--gua` → 予我 still **`hōo--guá`** (whole-buffer dictionary word wins, S-dict-roman); `hoo-gua` → 予我 as the walker's 予 + 我 `hōo-guá`, never the record's `hōo--guá` (S69: a plain `-` is not the khinsiann boundary the record carries); `hoogua` unchanged; pick 鵝仔是 from `goa--si` then again from `goasi` → the second pick still ranks first (one frequency key, the separator never forks identity); TPS layout unchanged; `--ah` → `--ah` / `--矣` (S8) unchanged; a partial-syllable buffer (`goa-s`) shows the same span-local rows as before.
- **S67 Frequency Records / Association Records pages removed, recording always on (iOS + Android)** (2026-09-22, USER: "remove the UI and related i18n of 'Frequency Records' and 'Association Records', but don't affect the functionality; I just want a clean, good-looking layout" → toggles retired, always on; PR #130, platform-only → no `make build`): Dictionary tab Data Management section lists **Custom Dictionary** and **Backup/Restore** only, no Frequency Records / Association Records rows, hanji/TL/POJ/en/ja display languages all clean (no missing-key placeholders). Type `tsiah` and pick 食 twice, then type `tsiah` again → 食 ranks first (frequency still records); commit 我 then 食 → after the next 我 commit the smartbar suggests 食 (association still records). **Upgrade control**: an install that had either toggle OFF before this build still records both after upgrade (no reset needed). Backup/Restore `.taigi` export → import still restores frequency + association counts; General › Reset to Defaults still clears both records and recording continues afterwards. Custom dictionary CSV import/export unchanged (desktop unaffected).
- **S68 Typed separator: committed between picks, remembered by the learned phrase (all four platforms)** (2026-09-22, USER: "the MOE dictionary orthography cannot cover every rule, so in most cases users will handle it themselves" → 「ok,plan and implement it」, then observed `tng--lai` two picks → `tng-lai`; PR #131, engine-only → `make build` first; macOS `make -C macos install`, Windows reinstall; sentences from `corpus/README.md`): TL, continuous input, romanization output. **Commit** (MOE dictionary #323 「伊昨昏三更半暝才轉來。」 `… tsiah tńg--lâi.`): type `tng--lai`, pick 轉 (single), then 來 (single) → the preedit after 轉 reads `tńg--lai` and the committed text is **`tńg--lâi`** (was `tńg-lâi`); `tng-lai` two picks → `tńg-lâi`; `tnglai` two picks → `tńg-lâi` (the dictionary compound, as before). **Learning** (a phrase the dictionary does not carry as one word — MOE dictionary #111 「我今仔入門，電話就來矣。」 `… tiān-uē tō lâi--ah.`): type `lai--ah`, pick 來 then 矣; clear, type `laiah` → 來矣 leads and reads **`lâi--ah`**; `lai-ah` two picks → later `laiah` reads `lâi-ah`; `laiah` two picks (nothing typed) → `lâi-ah`. **Correction**: compose 來矣 from `lai-ah` once, then from `lai--ah` once → `laiah` reads `lâi--ah` (tie → newest); twice more from `lai-ah` → `lâi-ah` again (most composed wins), only ONE 來矣 row in the strip. **Negative controls**: `tng---lai` two picks → commits `tńg---lâi` verbatim; `lai---ah` two picks → later `laiah` reads `lâi--ah` (learning keeps the kind); No Hyphens ON → `tng--lai` two picks commit `tńg·lâi`, `tng-lai` → `tńglâi`, learned `lâi--ah` renders `lâi·ah`; Enter after picking 轉 over `tng--lai` commits `tńg--lai`; type `tng--lai-khi`, pick 轉 then 來 (preedit `tńg--lâi-khi`), backspace ×4 clears `-khi`, one more backspace pops 來 and the preedit reads `tńg--lai`, pick 來 → `tńg--lâi`; Hanji output → `轉來` either way; picking 轉來 as ONE candidate still commits its own `tńg--lâi`; one whole-buffer pick (slot 0 over `tng--lai`) commits `tńg--lâi` and learns nothing, as before; frequency: pick the learned `lâi--ah` three times → it ranks first only under that form (the `lâi-ah` row keeps its own count).
- **S69 Typed hyphen is a syllable boundary (all four platforms)** (2026-09-22, USER: "a khinsiann separator typed by the user is an explicit word break… khi should be treated as one syllable, rather than treating khiah as one syllable"; engine-only → `make build` first; macOS `make -C macos install`, Windows reinstall; sentences from `corpus/README.md`): TL, continuous input. MOE dictionary #5655 「去市場拄著一隻飛鼠，錢就予伊撏去矣。」 `… tsînn tō hōo i jîm--khì--ah.` — type `jim--khi--ah`, pick 撏 (or 忍) as one syllable → the pending `--khi--ah` strip leads with **去** and lists **no 隙 / 屐**; pick 去 → pending `--ah` → 矣. Type `khi--ah` fresh → first hanji candidate 去矣 reads `khì--ah`, 去啊 present, 隙 absent; `khi-ah` → reads `khì-ah`, 隙 absent AND 去啊 absent (a plain `-` is not its `--`). POJ: `khi--ah` → same. **Kind** (USER: "match the separator kind"): `jim--khi` → first candidate 任去 `jīm--khì` (the walker's 任 + 去; the engine fixture reads 忍去 `jím--khì`), the compound 忍氣 not listed; `jim-khi` → 忍氣 `jím-khì` first; `ia-si` → 也是 `iā sī`; `ia--si` → 也是 as 也 + 是 `iā--sī` (no `iā sī` cell); `hoo--gua` → 予我 `hōo--guá`, `hoo-gua` → 予我 as 予 + 我 `hōo-guá` (S66 control revised); a custom 矣 stored `--ah`: `khi-ah` never renders `khì---ah`, `khi--ah` → 去矣 `khì--ah`. **Negative controls**: `khiah` (nothing typed) → 隙 leads and 屐 / 去啊 still listed; `khi3--ah` → the full-span candidates are 去矣 / 去啊 only (tone + boundary; span-local 去 / 氣 / 器 still list); `tania` / `taiuantai` unchanged; `iah8` (S56) unchanged and `iah-` also hides 也/抑-style `ia` singles while `iah` keeps them; a custom row `Khì--ah` (capital) typed as `khi--ah` still appears, a custom `Khiah` does not; TPS layout and TPS space separator unchanged (S18 / S23), and a TPS buffer ending in `-` still lists every tone (the OOV split is engine-test only — every syllable in the production dictionary has a row).
- **S70 "ⁿ becomes ᴺ in capitals" toggle (all four platforms)** (2026-09-22, USER: "add a settings toggle 'ⁿ capitalization', on by default… when off, always lowercase", label renamed "ⁿ becomes ᴺ in capitals" same day; engine + all four platforms → `make build` first; macOS `make -C macos install`, Windows reinstall): POJ. **Default ON**: the switch is present and ON on a fresh install (mobile POJ section under Double-tap nn → ⁿ, desktop General under No Hyphens); Caps Lock, type `siann5` → preedit **`SIÂᴺ`**, the 城 candidate reads **`SIÂᴺ`** too (was `SIÂⁿ` — parity correction, §53), picking it under romanization output commits `SIÂᴺ`, Enter commits `SIÂᴺ`; shift + `siann5` → `Siâⁿ`; mobile: the `nn` key label shows `ᴺ` while shifted. **OFF**: Caps Lock `siann5` → preedit **`SIÂⁿ`**, the 城 candidate `SIÂⁿ`, both commits `SIÂⁿ`; Caps Lock `siannim` → 聲音 `SIAⁿ-IM`; mobile `nn` key label stays `ⁿ` under shift, and a non-continuous POJ suggestion under Caps Lock reads `…ⁿ`. **Negative controls**: lowercase `siann5` → `siâⁿ` either way; TL `siann5` → `siânn` either way; TPS unchanged; Double-tap nn → ⁿ OFF (mobile) still leaves `nn` as two letters; Android long-press `n` → the `ⁿ` cell reads `ᴺ` under shift only while the switch is ON; toggling the switch changes nothing already committed and no candidate order; General › Reset to Defaults (desktop) puts it back ON; the same frequency row leads whichever way the switch is set (identity never forks on the marker's case).
- **S71 Typing after a nail keeps the Hanji-first prefix unspaced (iOS + Android)** (2026-09-22, retro Codex review of #103 → §54; platform-only, no `make build`): default Hanji-first, TL. Type `taigi`, pick `台` → preedit `台gi`; type `h` → **`台gih`, no space** (the old build read `台 gih`); Backspace → `台gi`; pick 語 → commits `台語`. Same with the hyphen key: `tai`, pick `台`, `-`, `uan` → `台-uan` (no space before `-`). No Hyphens ON, romanization-first: `taiuangua`, pick `tâi`, pick `uân` → prefix `tâiuân` (compound, no hyphen); type `h` → **`tâiuân guah`**, the prefix never re-hyphenates to `tâi-uân`; Backspace → `tâiuân gua`. **Negative controls**: romanization-first `taigi` → pick `tâi` → type `h` → `tâi gih` (space kept); Hanji with Romanization → `tâi (台) gih`; TPS layout unchanged; desktop already green (S37).
- **S72 Caps Lock keeps a POJ custom entry all caps (all four platforms)** (2026-09-22, retro Codex review of #89; engine → **`make build` first**; macOS `make -C macos install`, Windows reinstall): POJ mode, ⁿ becomes ᴺ in capitals ON, custom entry `Keng-lâm Su-īⁿ` for `klsi` (S50 setup). Caps Lock, type `KLSI` → the custom candidate reads **`KENG-LÂM SU-Īᴺ`** (the old build read `Keng-Lâm Su-Īᴺ`); pick it under romanization output → commits `KENG-LÂM SU-Īᴺ`. Switch OFF → `KENG-LÂM SU-Īⁿ`. Add a mixed-case custom entry `KENG-lâm` (any Hanji) → typed `kl` shows **`KENG-lâm`** as stored, `KL` under Caps Lock `KENG-LÂM`. **Negative controls**: `klsi` / `Klsi` → `Keng-lâm Su-īⁿ` (unchanged, S50); TL mode `KLSI` → `KENG-LÂM SU-ĪNN`; dictionary rows under Caps Lock unchanged (`SIANN` → `SIAᴺ`, S70); candidate order and the frequency row unchanged.
- **S73 Typed hyphen renders inside a dictionary word (all four platforms)** (2026-09-22, USER: "the user put a hyphen inside pang-tang; the user's form is expected to prevail" → 「ok,go」 → §55; engine-only → **`make build` first**; macOS `make -C macos install`, Windows reinstall; sentence from `corpus/README.md`): TL, continuous input. MOE dictionary #5517 「伊是專門咧放重利的。」 `I sī tsuan-bûn teh pàng-tāng-lāi--ê.` — type `pang-tang-lai` → 放重利 reads **`pàng-tāng-lāi`** (the old build read the record's `pàng tāng-lāi`); pick it under romanization output → commits `pàng-tāng-lāi`; `pang3-tang7-lai` and Caps Lock `PANG-TANG-LAI` (→ `PÀNG-TĀNG-LĀI`) same. `pang--tang-lai` → `pàng--tāng-lāi` (unchanged, walker 放 + 重利). POJ: `pang-tang-lai` → `pàng-tāng-lāi`. No Hyphens ON: `pang-tang-lai` → **`pàngtānglāi`** (the old build read `pàng tānglāi`). **Negative controls**: `pangtanglai` (nothing typed) → **`pàng tāng-lāi`** exactly as today; every S66 / S69 row unchanged (`goa-si` `gô-á-sī`, `goa--si` `gô-á--sī`, `hoo--gua` 予我 `hōo--guá`, `hoo-gua` 予 + 我 `hōo-guá`, `ki-khi-lai` 機起來, `ki--khi-lai` 記起來 `kì--khí-lâi`, `khi--ah` 去啊 `khì--ah`); pick 放重利 from `pang-tang-lai` then from `pangtanglai` → the second pick still ranks first (one frequency key); `tai-` (trailing hyphen) unchanged; TPS layout unchanged.
  Status: pending
- **S74 Linux first machine — Fcitx5 on KDE Plasma, then IBus on GNOME (Ubuntu 24.04 VM)** (2026-09-23, roadmap `linux-roadmap.md` § Dogfood run-book; PR0–PR9; `sudo apt install ./taigikeyboard_<version>_amd64.deb` from the desktop draft or `make -C linux deb`): (0) fresh session after the install (log out / in), the app grid shows TaigiKeyboard with the 台 icon and opens the settings window; (1) `fcitx5 -r` → TaigiKeyboard appears in `fcitx5-configtool` under Taiwanese (`nan`); add it, switch to it, and the first key loads the addon (the Fcitx5 log names `libtaigikeyboard.so`, no "Could not locate library"); (2) S1 POJ diacritics + S3 candidate paging on the Fcitx5 panel in Kate, Konsole, Firefox — the slot-key labels match the Shortcuts pane, page arrows / scroll page the list and the slot keys still pick off the page shown; (3) focus loss commits the preedit; (4) Ctrl+Alt+S opens the settings window on the last pane, a second launch re-activates it on `--pane`; (5) Ctrl+Alt+C / bare `` ` `` switch and the tray / status-area text (`台羅` / `白話`) plus the framework's information notice (`台羅 · 漢字優先`) follow; Ctrl+Alt+/ shows the Telex guide as a page and the next key takes it down; Ctrl+Alt+, opens the symbol picker, Enter commits the highlighted symbol and it leads the next opening; (6) every General / Appearance / Shortcuts / Dictionary Sources row round-trips through `settings.json` within a second in both directions; a chord recorded in Shortcuts fires on the next key; (7) Custom Dictionary: add / edit through the entry sheet (Save greyed until a romanization is typed), delete (no prompt — one row), CSV import + export through the file chooser, delete all and Clear Learning Records each behind a destructive confirmation; the outcome shows as a toast; (8) then GNOME + IBus: `ibus restart` → Settings › Keyboard › Input Sources lists TaigiKeyboard with the `台羅` indicator; the panel menu shows the two switch rows / Settings / About; the Telex guide shows no `1…9` labels; (9) `apt remove taigikeyboard` keeps `~/.config/taigikeyboard` and `~/.local/share/taigikeyboard`, and a reinstall finds the same settings, custom entries and learned words. (10) on both shells: a password field (Firefox login page, a GNOME Settings password prompt) takes letters as plain ASCII, no preedit, no candidates, and the next text field composes again; Ctrl+Alt+, does not open the picker there; the panel menu's second switch row is Candidate Display Mode (the label's second half changes), not Output the Other Script. (11) shell parity (2026-09-24): on GNOME + IBus with Shortcuts › Selection Keys = Digits, `tai` then Shift+2 commits the 2nd cell in its other script and Shift+Q types `Q` (no candidate committed); with the bare-key set Shift+V types `V` and Shift+; commits the 9th cell's other script; on both shells a composition is WRITTEN, not dropped, when the input method is switched mid-word (Ctrl+Space / Super+Space) and when the caret is clicked elsewhere in the text, and it is written once, not twice; `fcitx5-configtool` › TaigiKeyboard has a configure button whose Settings row opens the settings window. **Negative controls**: a key the engine does not consume reaches the app unchanged; English typed with another engine is untouched; `TAIGIKEYBOARD_DATA_DIR` unset reads `/usr/share/taigikeyboard/dictionaries`.
  Status: pending (no Linux machine until the VM; roadmap L12)
- **S75 One size pop-up for the candidate window, Standard a step smaller (macOS + Windows)** (2026-09-23, USER: "'Candidate Text Size / Candidate Window Size' 1. evaluate merging them into one fixed-ratio zoom 2. five steps in all: 'Extra Small', 'Small', 'Standard', 'Large', 'Extra Large', or simply evaluate a slider 3. the current Standard size is still too big" → 13 / 15 / 17 / 20 / 23 pt, old choices mapped → 「ok,go」 → "the sizes are fine, but I'd like to go back to the original drop-down menu"; platform + `desktop/` core, no engine change → no `make build`; macOS `make -C macos install`, Windows reinstall): Settings › Appearance shows ONE row **Candidate Window Size**, a pop-up of five steps (Extra Small · Small · Standard · Large · Extra Large) like the rows around it; the old Candidate Text Size row is gone. Fresh install (or after Reset to Defaults) → the pop-up reads **Standard**, and typing `taigi` shows a visibly smaller window than v3.6.9 (17 pt text; macOS row 26 pt, was 30). Pick each of the five → each step resizes the NEXT candidate window (text and the air around it together, same proportion). **Upgrade mapping**: an install that had chosen Small → Small (15 pt, was 16), Standard (explicitly chosen) → Large (20 pt, unchanged font size), Large → Extra Large (23 pt, unchanged font size) — the air around the text follows the new ratio, so the window height moves a little; an install that never touched it → the new Standard. **Legibility**: at Extra Small the digit hints (8 pt) and POJ diacritics (`siâⁿ`, `tsha̍h`) are still readable, nothing clips in expanded / horizontal / vertical layouts nor in side-by-side two-line cells; a custom typeface (Manage Typefaces) at Extra Small and Extra Large does not clip. **Negative controls**: the old Candidate Window Size air setting has no effect any more (a stored `candidateWindowSize` changes nothing); Appearance · Candidate Window Layout · Candidate Display rows unchanged; the Telex guide and the mode flash keep their own sizes; Linux (S74) unaffected — its panel is the framework's.
  Status: pending
- **S76 Linux bundled typefaces are system fonts; no Manage Typefaces pane (Ubuntu VM, both shells)** (2026-09-24, USER: "keep the Manage Typefaces page; for linux, add a short explanation" → Discord report "picking a typeface there leaves the panel unchanged" → "ok, for linux, remove the Manage Typefaces page"; Linux only, no engine change → `make -C linux install` or the `.deb`): (1) before the install, typing `tai` shows `𧉟` (U+2725F) as a box in the panel; after it, `fc-list ":charset=2725f" family` names the four bundled families, and after logging out and in the GNOME / Fcitx5 panel draws `𧉟`; `fc-match "sans-serif:lang=zh-tw"` still answers Noto Sans CJK TC (the bundled faces are fallbacks, not the default). (2) Settings sidebar ends at Custom Dictionary — no Manage Typefaces row; `taigikeyboard-settings --pane fontManagement` opens General. (3) Fcitx5 › Addons › Classic User Interface › Font lists 粉圓 · 芫荽 · 源樣明體 · 源樣烏體; one picked there draws the panel at once. **Negative controls**: macOS / Windows Manage Typefaces unchanged (selection pane); a typeface added earlier under `~/.local/share/fonts/taigikeyboard/` still lists in `fc-list`; `apt remove taigikeyboard` removes the four bundled files.
- **S77 Identical desktop menus; no update check on Linux (Ubuntu VM, KDE + Fcitx5 then GNOME + IBus; macOS; Windows)** (2026-09-25, USER: "I want the macos, windows and linux menus to have identical contents, including i18n", then "for desktop, only macos and windows need the update check; linux doesn't, it can be removed entirely"; roadmap § Linux update check phase 4 + removal; install the PR's `.deb` or `make -C linux install`): (1) the macOS input-source menu and the Windows tray menu read Switch Tâi-lô or Pe̍h-ōe-jī · Switch Candidate Display | TaigiKeyboard Settings | Check for Updates · About the Keyboard (English UI: "TaigiKeyboard Settings"); the Linux panel menu reads the same rows without Check for Updates; (2) Linux General's version row shows the running version and Download, which opens taigikeyboard.tw; (3) Linux never checks: focus text fields for a while with the network up → no desktop notification, `pgrep -f 'taigikeyboard-settings --check'` finds nothing, and `~/.config/taigikeyboard/settings.json` has no `updateNextCheckMs`. **Negative controls**: macOS / Windows menu Check for Updates still answers (You're up to date or Update Available); the Linux packages link no libssl (`ldd /usr/bin/taigikeyboard-settings`).
  Status: pending
- **S78 Custom theme: one Key Fill row, white by default (iOS + Android)** (2026-09-26, USER: "custom theme function keys should default to white", then "merge them: changing the key colour changes every key, no normal / special split"; no engine change, no `make build`; app build only): (1) Theme › New Theme… → the Keys section reads Key Fill · Key Text · Key Corner Radius … (no Special Key Fill row); the preview shows white letter keys **and** white ⇧ / ⌫ / 123 / globe / Return; (2) pick Key Fill = blue → every preview key turns blue, including the function keys; tap the row's reset arrow → all white again; Reset to Defaults → all white; (3) save → the real keyboard in Notes shows the same single fill on every key. **Old themes**: a custom theme saved before this build with a grey special fill now shows its letter-key colour on the function keys too, in the card preview, the editor and the keyboard. **Negative control**: built-in themes (Classic Default, Sakura, Catppuccin) keep their distinct function-key fill.
  Status: pending
- **S79 Custom theme card: background only, no sample key (iOS + Android)** (2026-09-26, USER: "after saving a custom theme, the preview's checkmark doesn't need a square frame around it" → "no sample-key preview needed, only the checkmark and the background preview"; no engine change, no `make build`; app build only): (1) Theme › Custom Themes → every saved theme's card shows only its background (solid / gradient / photo), no centred sample key; (2) save a new theme → it auto-applies and its card shows the dim mask + blue circle checkmark straight on the background, with no white rounded box behind the checkmark; (3) tap another custom card → the checkmark moves; its background still reads correctly (photo dim, gradient direction). **Negative control**: built-in cards (Filled / Outlined / Borderless shelves) still show their screenshots and the same checkmark.
  Status: pending
- **S80 Custom theme photo: decoded off the main thread (iOS + Android)** (2026-09-26, USER: "check the custom theme feature for jank / performance problems" → "ok, go" on moving photo decode off the main thread; no engine change, no `make build`; app build only). Set up five custom themes, each with a different photo background. (1) Theme tab: kill the app, reopen, open Theme → the tab opens without a hitch; photo cards may show grey for an instant, then their photos (small thumbnails, not blurry); scrolling the shelf stays smooth. (2) Keyboard: apply a photo theme, force-stop / swipe away the host app (Android: also Force stop the app to kill the IME), open Notes and show the keyboard → it slides in immediately; the surface may be grey for an instant before the photo appears; type S1 (POJ diacritics) and S3 (Hanji candidate scroll) → no stutter. (3) Switch between two photo themes several times and reopen the keyboard each time → always the right photo, never the previous theme's (A→B→A). (4) Editor: open a photo theme → the preview photo and the Photo row thumbnail appear; pick a new photo → it lands without a freeze; drag to reposition → smooth. (5) Open the symbol / layout / settings panels and the expanded candidate list over a photo theme → each shows the photo slice (grey for an instant at most). **Memory**: iOS: switch among the five photo themes ~10 times while typing → no keyboard dismissal (64 MB cap). **Negative control**: solid and gradient themes, and built-in themes, render exactly as before (never grey).
  Status: pending
