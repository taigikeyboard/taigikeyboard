# Telex 聲調鍵 mapping(改善提案,2026-09-12)

## 現況(desktop 3.6.8 已出)

| 鍵 | 功能 | TL 例 | POJ 例 |
|---|---|---|---|
| `v` | 第 2 調 | `tev` → té | `pav` → pá |
| `y` | 第 3 調 | `tey` → tè | `pay` → pà |
| `d` | 第 5 調 | `ted` → tê | `pad` → pâ |
| `w` | 第 7 調 | `tew` → tē | `paw` → pā |
| `x` | 第 8 調 | `titx` → ti̍t | `chitx` → chi̍t |
| `q` | 第 9 調 | `tsangq` → tsa̋ng | `changq` → chăng |
| `z` / `zh` | ts / tsh | `zi` → tsi、`zhi` → tshi | `zi` → chi、`zhi` → chhi |
| `f` | 連字號 `-` | `taidfgiv` → tâi-gí | 同 |
| `1`–`9` | 選候選詞 | | |
| — | 第 1 調 | **無鍵** | |
| — | 第 4 調 | **無鍵** | |
| — | 第 6 調 | 無鍵(無空字母) | |

## 提案

原則:入聲字(尾音 `p / t / k / h`)只有 4、8 調;非入聲字只有 1、2、3、5、7、9 調。同一鍵看尾音自動分。

| 鍵 | 非入聲(尾音非 p/t/k/h) | 入聲(尾音 p/t/k/h) | TL 例 | POJ 例 |
|---|---|---|---|---|
| `c` **新** | 第 1 調 | 第 4 調 | `taic` → tai、`sitc` → sit | `taic` → tai、`chitc` → chit |
| `v` | 第 2 調(不變) | 第 8 調(**由 `x` 併入**) | `tev` → té、`titv` → ti̍t | `pav` → pá、`chitv` → chi̍t |
| `y` | 第 3 調 | — | `tey` → tè | `pay` → pà |
| `d` | 第 5 調 | — | `ted` → tê | `pad` → pâ |
| `w` | 第 7 調 | — | `tew` → tē | `paw` → pā |
| `q` | 第 9 調 | — | `tsangq` → tsa̋ng | `changq` → chăng |
| `x` | **空出來**(用途未定) | | | |
| `z` / `zh` | ts / tsh(不變) | | | |
| `f` | `-`(不變) | | | |
| `1`–`9` | 選候選詞(不變) | | | |

## `c` 鍵的例外(避免跟 POJ `ch` 打架)

| 打到一半的內容 | 按 `c` 的結果 |
|---|---|
| 什麼都還沒打、或剛打完 `-`、或這個音節已經有聲調 | 當普通字母 `c`(POJ `chia`、`tai5chia`、`tai-chia` 照打) |
| 完整音節、還沒標調 | 標調(1 或 4) |

殘留歧義只有一個:`tai` 直接接 `c` 想打 POJ `chia` → 會變 `tai` 第 1 調。解法:POJ 的 ch 改用 `z`(Telex 本來就有),或先幫前一個音節標調。

## 為什麼比原本方案好

| | 原方案(2026-09-11) | 提案 |
|---|---|---|
| 新增鍵 | 2 個(`c`=1、`r`=4) | 1 個(`c`) |
| `r` 鍵 | 要特別規則閃 `ir`/`er` 方言韻 | 完全不碰 |
| `x` 鍵 | 仍是第 8 調 | 空出來 |
| 第 8 調 | `x` | `v`(入聲字自動判) |

## 待決定

1. 空出的 `x`(及 `r`)用途:第 6 調 / 一鍵 `tsh` / 先空著。
2. `x` 要不要暫時保留當第 8 調(相容 3.6.8 已在用的人)。
